# Cloud Fundamentals

## Cloud computing concepts
- Cloud computing delivers computing services—including servers, storage, databases, networking, software, analytics, and intelligence—over the internet ("the cloud") to offer faster innovation, flexible resources, and economies of scale
- The core concept involves pooling computing resources to serve multiple consumers using a multi-tenant model, with different physical and virtual resources dynamically assigned and reassigned according to consumer demand
- On-demand self-service enables consumers to provision computing capabilities automatically without requiring human interaction with service providers
- Broad network access means capabilities are available over the network and accessed through standard mechanisms that promote use by heterogeneous thin or thick client platforms
- Resource pooling allows provider's computing resources to be pooled to serve multiple consumers using a multi-tenant model, with different physical and virtual resources dynamically assigned and reassigned according to consumer demand
- Rapid elasticity enables capabilities to be elastically provisioned and released to scale rapidly outward and inward commensurate with demand
- Measured service means cloud systems automatically control and optimize resource use by leveraging a metering capability at some level of abstraction appropriate to the type of service
- Virtualization is the foundational technology that enables cloud computing by abstracting physical hardware and allowing multiple virtual machines to run on a single physical server
- Abstraction separates the management layer from the hardware layer, enabling administrators to manage resources independently of the underlying physical infrastructure
- Orchestration automates the arrangement, coordination, and management of complex computer systems, middleware, and services in cloud environments

## Evolution of cloud computing
- Mainframe computing (1950s-1960s) introduced time-sharing and multiple user access to centralized computing resources, establishing concepts later used in cloud computing
- Client-server architecture (1970s-1980s) distributed processing between clients requesting services and servers providing them, creating the foundation for distributed computing models
- Virtualization technology (1990s) revolutionized data centers by allowing multiple operating systems to run on a single physical machine, significantly improving hardware utilization
- Application Service Providers (ASPs) emerged in the late 1990s, delivering software applications over the internet, foreshadowing modern SaaS models
- Amazon Web Services launched in 2006 with Simple Storage Service (S3) and Elastic Compute Cloud (EC2), marking the beginning of modern IaaS cloud computing
- Google App Engine launched in 2008 as one of the first PaaS offerings, abstracting infrastructure management and allowing developers to focus solely on code
- Microsoft Azure launched in 2010, providing comprehensive IaaS and PaaS capabilities integrated with Microsoft's enterprise software ecosystem
- Hybrid cloud models evolved around 2012-2015 as enterprises sought to balance on-premises control with public cloud flexibility and scalability
- Serverless computing emerged around 2014-2016 with AWS Lambda, abstracting even server management and enabling pure event-driven, function-based architectures
- Multi-cloud and distributed cloud strategies have become prevalent since 2018, with organizations leveraging multiple providers and edge computing for optimization and resilience

## IaaS vs PaaS vs SaaS
- Infrastructure as a Service (IaaS) provides on-demand access to IT infrastructure—servers, virtual machines, storage, networks—for tasks such as hosting applications, running workloads, and managing data while maintaining control over operating systems and applications
- IaaS offers the highest level of flexibility and management control, making it ideal for complex, custom applications, lift-and-shift migrations, and organizations with specific compliance or security requirements
- Platform as a Service (PaaS) delivers a framework for developers to build and deploy custom applications without managing the underlying infrastructure, including operating systems, databases, and middleware
- PaaS accelerates development and deployment of applications by providing pre-built tools, services, and frameworks, reducing the complexity of managing development stacks and infrastructure
- PaaS offerings typically include development tools, database management systems, middleware, and business analytics services integrated into a single platform
- Software as a Service (SaaS) delivers complete software applications over the internet on a subscription basis, eliminating the need for organizations to install, manage, and maintain software
- SaaS provides the lowest level of control but highest level of convenience, with providers managing everything from infrastructure to application data and security
- Key comparison factors include control level (highest in IaaS, lowest in SaaS), management responsibility (user manages applications and data in IaaS, provider manages everything in SaaS)
- Pricing models differ: IaaS typically charges for compute, storage, and network usage; PaaS charges for platform services and resources consumed; SaaS charges per-user subscription fees
- Use case alignment: IaaS for custom infrastructure and lift-and-shift, PaaS for application development and deployment, SaaS for ready-to-use business applications

## Public vs private vs hybrid cloud
- Public cloud is owned and operated by third-party cloud service providers who deliver computing resources over the internet, with all hardware, software, and supporting infrastructure managed by the provider
- Public cloud advantages include eliminating capital expenditure, unlimited scalability, rapid deployment, and no infrastructure management burden, with costs shared across multiple tenants
- Public cloud challenges include potential security concerns, limited control over infrastructure, compliance considerations for regulated data, and potential for unexpected costs
- Private cloud consists of computing resources used exclusively by a single organization, which can be located on-premises or hosted by a third-party service provider
- Private cloud advantages include greater control, enhanced security and compliance capabilities, predictable performance, and the ability to customize infrastructure to specific needs
- Private cloud challenges include higher costs, increased management responsibility, limited scalability compared to public cloud, and requirement for specialized IT skills
- Hybrid cloud combines public and private clouds, allowing data and applications to be shared between them, enabling organizations to gain the benefits of both models
- Hybrid cloud enables workload portability, orchestration, and management across environments, allowing organizations to keep sensitive data in private cloud while leveraging public cloud for burst capacity
- Key hybrid cloud use cases include cloud bursting (using public cloud during peak demand), disaster recovery, data backup, and running workloads where they are most cost-effective
- Hybrid cloud challenges include complexity in integration, network latency considerations, security consistency across environments, and requiring robust management tools

## Multi-cloud strategies
- Multi-cloud strategy involves using multiple cloud services from different providers—such as AWS, Azure, and GCP—for different workloads or as redundancy for critical applications
- Avoiding vendor lock-in is a primary driver, allowing organizations to maintain negotiating power and flexibility to switch providers when advantageous
- Best-of-breed approach enables organizations to select the most suitable services from each provider based on specific workload requirements and performance characteristics
- Geographic distribution benefits include leveraging different providers' regional presence to optimize latency and comply with data sovereignty requirements
- Risk mitigation through diversification reduces impact of provider outages, service disruptions, or security incidents affecting a single cloud provider
- Cost optimization opportunities arise from comparing pricing models and taking advantage of different providers' pricing structures and commitment discounts
- Compliance and regulatory requirements may necessitate using specific providers in certain regions or for particular data classifications
- Technical challenges include increased complexity in management, security, networking, and monitoring across multiple platforms requiring specialized skills
- Governance complexity increases as organizations must maintain consistent policies, access controls, and compliance frameworks across disparate cloud environments
- Multi-cloud management tools and abstraction layers help address challenges by providing unified visibility, orchestration, and policy enforcement across providers

## Cloud service models
- Infrastructure services provide fundamental computing resources like virtual machines, block storage, and virtual networks, with customers responsible for managing operating systems and applications
- Compute services include virtual machines, containers, and serverless functions that execute application code and process workloads in the cloud
- Storage services encompass object storage, block storage, file storage, and archival storage, each optimized for different data access patterns and durability requirements
- Database services range from relational databases to NoSQL databases, managed services, and data warehouses, abstracting database administration tasks
- Networking services include virtual networks, load balancers, firewalls, DNS, and content delivery networks that connect and protect cloud resources
- Platform services provide application hosting, development tools, integration services, and middleware that enable developers to build and deploy applications
- Analytics services include data processing, business intelligence, machine learning, and data visualization tools that derive insights from data
- Integration services facilitate connectivity between applications and data sources through APIs, message queues, event buses, and workflow orchestration
- Security and identity services manage authentication, authorization, encryption, and threat detection across cloud environments
- Management and governance services provide monitoring, logging, cost management, and compliance tools for operating cloud environments

## Cloud deployment models
- Public cloud deployment makes resources available to the general public over the internet, with infrastructure owned and operated by the cloud provider at their data centers
- Private cloud deployment dedicates cloud infrastructure to a single organization, which may be managed internally or by a third party, and hosted on-premises or externally
- Community cloud deployment shares infrastructure between several organizations with common concerns—such as security, compliance, or jurisdiction—for shared benefits
- Hybrid cloud deployment combines two or more distinct cloud infrastructures (public, private, or community) that remain unique entities but bound by standardized technology
- Multi-cloud deployment involves using multiple public cloud services from different providers, with workloads distributed across AWS, Azure, GCP, and others
- Distributed cloud deployment extends public cloud infrastructure to different physical locations while maintaining centralized management and control
- Edge cloud deployment places computing resources at the edge of the network, closer to data sources and users, to reduce latency and bandwidth usage
- Sovereign cloud deployment ensures data residency and compliance with local laws by operating cloud services within specific geographic boundaries
- Telco cloud deployment optimizes cloud infrastructure for telecommunications workloads, supporting network functions virtualization and edge computing
- Bare metal cloud deployment provides dedicated physical servers without virtualization, offering raw performance for specialized workloads while maintaining cloud-like provisioning

## Shared responsibility model
- Shared responsibility model defines security and compliance responsibilities between cloud providers and customers, which varies depending on the service model (IaaS, PaaS, SaaS)
- Provider is always responsible for physical security of data centers, hardware, software, and networking that comprise the cloud infrastructure globally
- Provider manages the virtualization layer, including hypervisors, which isolates customer workloads and prevents unauthorized access between tenants
- Provider maintains global infrastructure availability and durability, ensuring core services meet published SLAs and redundancy requirements
- For IaaS, customers are responsible for managing guest operating systems, applications, data, network configurations, firewall settings, and identity access management
- For PaaS, customers focus on their applications and data while providers handle runtime environments, middleware, operating systems, and infrastructure
- For SaaS, customers typically only manage their data, user access, and application configurations, with provider managing everything else
- Data classification and accountability remains with customers regardless of service model, including where data is stored and who can access it
- Identity and access management is a shared area where providers offer tools but customers must implement proper authentication and authorization policies
- Compliance certifications and attestations are provided by cloud vendors, but customers must ensure their use of services meets specific regulatory requirements

## Cloud economics
- Capital expenditure (CapEx) shifts to operational expenditure (OpEx) when moving to cloud, replacing upfront hardware purchases with pay-as-you-go consumption-based costs
- Total Cost of Ownership (TCO) analysis compares direct and indirect costs of on-premises infrastructure versus cloud solutions over time
- Economies of scale enable cloud providers to achieve lower costs than individual organizations through massive infrastructure purchasing power and operational efficiency
- Variable costs replace fixed costs, allowing organizations to pay only for resources consumed rather than maintaining capacity for peak demand
- Reduced time-to-market accelerates business value by eliminating procurement cycles and infrastructure deployment delays
- Labor cost optimization reduces staff required for hardware maintenance, data center operations, and infrastructure management
- Energy and facility cost elimination removes expenses for power, cooling, physical security, and data center space
- Resource optimization through rightsizing ensures organizations don't overpay for underutilized resources and can scale down when demand decreases
- Reserved instances and savings plans provide significant discounts for committed usage, reducing costs for predictable workloads
- Spot instances and preemptible VMs offer steep discounts for fault-tolerant, interruptible workloads, maximizing cost efficiency

## Pay-as-you-go pricing
- Pay-as-you-go model charges customers based on actual resource consumption without upfront commitments or long-term contracts
- Compute pricing typically charges per second or per hour of virtual machine usage, with variations based on instance type, region, and operating system
- Storage pricing depends on data volume stored, storage class or tier, and data access patterns including retrieval frequency and data transfer
- Data transfer pricing includes charges for data moving between regions, to the internet, or between availability zones, while ingress often remains free
- API request pricing applies to many services, charging per operation such as S3 PUT/GET requests or Lambda function invocations
- Reserved capacity pricing offers discounted rates in exchange for committing to use specific resources for one or three-year terms
- Spot pricing allows bidding on unused capacity, with prices fluctuating based on supply and demand, and instances potentially reclaimed with notice
- Tiered pricing provides volume discounts where per-unit costs decrease as usage increases across compute, storage, or data transfer
- Sustained use discounts automatically apply for extended usage periods, particularly in Google Cloud's compute pricing model
- Free tiers provide limited usage at no cost, allowing organizations to experiment and run small workloads without charges

## Elasticity and scalability
- Elasticity refers to the ability to automatically scale resources up or down based on real-time demand, matching capacity to current requirements
- Horizontal scaling (scaling out) adds more instances of a resource, distributing load across multiple servers for increased capacity and fault tolerance
- Vertical scaling (scaling up) increases the capacity of existing instances by adding more CPU, memory, or storage to handle larger workloads
- Auto-scaling automatically adjusts compute capacity based on policies, schedules, or real-time metrics like CPU utilization or request count
- Predictive scaling uses machine learning to forecast traffic and proactively scale resources before demand increases
- Scheduled scaling adjusts capacity at predetermined times to accommodate known patterns like end-of-month processing or seasonal peaks
- Manual scaling provides direct control for administrators to adjust resources when automatic policies aren't suitable or during planned events
- Load balancing distributes traffic across scaled resources, ensuring even distribution and high availability during scaling events
- Database scaling involves techniques like read replicas, sharding, and connection pooling to handle increased query volume
- Scaling considerations include startup time for new instances, state management, and ensuring applications are designed to handle dynamic environments

## High availability concepts
- High availability ensures systems remain operational and accessible despite component failures, typically measured as percentage of uptime over time
- Availability targets are expressed as "nines"—99.9% (three nines) allows ~8.76 hours downtime yearly, while 99.999% (five nines) allows ~5.26 minutes
- Redundancy eliminates single points of failure by deploying multiple instances of critical components across different failure domains
- Active-active configurations distribute workload across multiple running instances, maximizing resource utilization and providing seamless failover
- Active-passive configurations maintain standby instances that take over when primary fails, potentially simpler but with some failover delay
- Failover automatically transfers operations to redundant systems when primary systems fail, requiring detection, routing changes, and state synchronization
- Health monitoring continuously checks system components to detect failures quickly and trigger appropriate remediation actions
- Graceful degradation ensures core functionality remains available even when non-critical components fail, maintaining essential user experience
- Maintenance windows for updates and patches must be considered in HA design, with rolling updates enabling zero-downtime maintenance
- Geographic distribution across regions protects against region-wide outages but introduces complexity in data synchronization and latency

## Fault tolerance
- Fault tolerance enables systems to continue operating correctly even when some components fail, without any interruption in service
- Redundancy at multiple levels—hardware, network, power, data—ensures that failures in individual components don't cause system failure
- Replication maintains identical copies of data or services across different physical locations to withstand localized failures
- Failover mechanisms automatically redirect traffic and operations from failed components to healthy ones without manual intervention
- Graceful degradation allows systems to maintain partial functionality when complete operation isn't possible, prioritizing critical functions
- Isolation prevents failures from cascading through systems, using techniques like bulkheads, circuit breakers, and rate limiting
- Recovery mechanisms include automated restart of failed processes, reconstruction of corrupted data, and reintegration of recovered components
- Byzantine fault tolerance addresses arbitrary failures including malicious behavior, requiring consensus among distributed components
- Testing fault tolerance through chaos engineering deliberately introduces failures to validate system resilience and recovery capabilities
- Cost considerations require balancing fault tolerance levels against business requirements, as five-nines availability becomes exponentially expensive

## Disaster recovery fundamentals
- Disaster recovery encompasses policies, procedures, and technologies for restoring critical systems and data after catastrophic failures
- Recovery Time Objective (RTO) defines maximum acceptable downtime for restoring systems after disaster, driving infrastructure and process requirements
- Recovery Point Objective (RPO) defines maximum acceptable data loss measured in time, determining backup frequency and replication requirements
- Backup and restore approach uses regular backups to stored snapshots, providing cost-effective recovery but longer RTO for full system restoration
- Pilot light maintains minimal core infrastructure running in recovery region, allowing rapid scale-up when disaster strikes the primary region
- Warm standby runs reduced-capacity production environment in recovery region, enabling faster failover than pilot light with moderate costs
- Multi-site active-active runs production workloads simultaneously across regions, providing near-zero RTO and RPO at highest cost
- Data replication synchronously or asynchronously copies data to recovery region, balancing data loss risk against performance impact
- Disaster recovery testing through regular drills validates procedures, identifies gaps, and ensures team readiness for actual events
- Automation of failover processes reduces recovery time and eliminates human error during high-stress disaster situations

## Regions and availability zones
- Regions are geographically distinct locations containing multiple, isolated Availability Zones, providing global footprint for cloud services
- Availability Zones consist of one or more discrete data centers with independent power, cooling, and networking, isolated from failures in other zones
- Latency between Availability Zones in the same region is typically under 2 milliseconds, enabling synchronous replication and distributed applications
- Region selection considerations include data residency requirements, compliance regulations, latency to users, and service availability
- Multi-region deployments provide resilience against region-wide disasters but introduce challenges in data consistency and cross-region latency
- Edge locations form content delivery networks, caching data close to users for low-latency access without full regional infrastructure
- Local zones extend regions to metropolitan areas for ultra-low latency applications requiring single-digit millisecond response times
- Wavelength zones embed AWS compute at 5G network edges, enabling applications requiring single-digit millisecond latency to mobile devices
- Outposts bring AWS infrastructure on-premises for workloads requiring low latency to on-premises systems or local data processing
- Government regions provide physically isolated infrastructure meeting specific compliance requirements for public sector workloads

## Cloud SLAs
- Service Level Agreements are formal contracts between cloud providers and customers defining expected service performance and availability commitments
- Availability commitments typically range from 99.5% to 99.99% depending on service tier, with premium tiers offering higher guarantees
- Financial credits (service credits) are provided when SLAs aren't met, typically as percentage discounts on future bills based on actual uptime
- Exclusions and limitations specify circumstances where SLA doesn't apply, including scheduled maintenance, force majeure, and customer actions
- Composite SLA calculation for multi-service applications requires combining individual service SLAs, potentially resulting in lower overall availability
- Multi-region deployments can achieve higher effective availability by routing around regional failures despite individual regional SLAs
- Performance SLAs address metrics beyond availability including latency, throughput, and request rates for specific services
- Measurement and reporting defines how service levels are monitored, verified, and reported to customers, typically through service health dashboards
- Notification requirements specify provider obligations for incident communication, maintenance windows, and service status updates
- Third-party verification through independent auditors validates provider compliance with published SLAs and operational practices

## Cloud compliance basics
- Compliance frameworks (ISO 27001, SOC 1/2/3, PCI DSS, HIPAA, FedRAMP) provide standardized security and privacy controls for cloud services
- Data residency requirements mandate that certain data must remain within specific geographic boundaries to comply with local laws
- Shared responsibility extends to compliance, with providers responsible for infrastructure compliance and customers for their use of services
- Compliance certifications are obtained by cloud providers through third-party audits, validating their controls meet framework requirements
- Customer compliance obligations include configuring services properly, managing access controls, and protecting data according to requirements
- Audit rights enable customers to review provider compliance evidence, often through access to audit reports rather than direct inspection
- Compliance automation tools help customers maintain compliance by continuously monitoring configurations and generating evidence
- Industry-specific regulations (GDPR, HIPAA, FedRAMP) impose additional requirements for handling protected data in specific sectors
- Compliance inheritance allows customers to leverage provider certifications for their own compliance programs, reducing audit burden
- Continuous compliance monitoring through cloud-native tools detects violations and generates alerts for non-compliant configurations

## Cloud governance
- Cloud governance establishes policies, procedures, and controls for managing cloud resources effectively, securely, and cost-efficiently
- Resource hierarchy organizes cloud assets into logical groups (organizations, folders, projects) for applying policies and managing access
- Policy as code defines governance rules programmatically, enabling automated enforcement and consistent application across environments
- Tagging and labeling strategies attach metadata to resources for cost allocation, ownership tracking, and compliance classification
- Budget controls and alerts prevent cost overruns by notifying stakeholders when spending approaches or exceeds thresholds
- Service limits and quotas prevent resource exhaustion or unexpected bills by cording maximum resource consumption
- Compliance frameworks integrate with governance to ensure resources meet regulatory and security requirements continuously
- Access governance ensures least-privilege principle through identity management, role-based access control, and regular access reviews
- Change management controls govern infrastructure modifications through approval workflows and automated compliance checks
- Governance automation continuously monitors, detects violations, and remediates non-compliant configurations without human intervention

## Cloud adoption frameworks
- Cloud Adoption Framework (CAF) provides structured guidance, best practices, and tools for organizations planning and executing cloud adoption
- AWS CAF organizes guidance into six perspectives: Business, People, Governance, Platform, Security, and Operations, each addressing specific stakeholder concerns
- Microsoft CAF for Azure includes strategy, plan, ready, adopt, govern, and manage phases with detailed methodologies for each stage
- Google CAF focuses on four themes: Learn, Lead, Scale, and Secure, helping organizations transform through cloud adoption
- Business perspective addresses financial management, strategy alignment, and value realization from cloud investments
- People perspective focuses on organizational change management, training, and culture transformation for cloud adoption
- Governance perspective establishes policies, controls, and management processes for maintaining oversight of cloud environments
- Platform perspective guides technical implementation, including landing zones, architecture patterns, and workload migration
- Security perspective ensures protection of data, identities, and applications throughout cloud adoption journey
- Operations perspective defines processes for running, monitoring, and optimizing cloud workloads post-migration

## Cloud migration strategies
- Migration strategies (the "6 Rs") provide standardized approaches for moving applications to cloud based on business and technical requirements
- Rehost (lift-and-shift) moves applications to cloud without modifications, providing quick migration with minimal changes but missing cloud optimization benefits
- Replatform (lift-tinker-and-shift) makes targeted cloud optimizations like moving to managed databases while keeping core application architecture
- Refactor/Re-architect modifies applications to use cloud-native features, maximizing cloud benefits but requiring more time and investment
- Repurchase replaces existing applications with SaaS alternatives, trading customization for reduced management overhead
- Retire decommissions applications no longer needed, eliminating maintenance costs and reducing migration scope
- Retain keeps applications on-premises temporarily or permanently due to technical, regulatory, or business constraints
- Migration waves group applications for phased migration based on dependencies, complexity, and business priorities
- Application discovery and assessment evaluates existing applications to determine appropriate migration strategies
- Post-migration optimization continuously improves migrated workloads through rightsizing, architecture enhancements, and cost management

## Lift-and-shift vs refactor
- Lift-and-shift migrates applications to cloud with minimal changes, preserving existing architecture while changing infrastructure location
- Lift-and-shift advantages include fastest migration timelines, lowest initial risk, minimal application changes, and preserving existing skills
- Lift-and-shift disadvantages include missing cloud optimization benefits, potential higher costs without rightsizing, and limited scalability improvements
- Refactoring modifies applications to use cloud-native services and architectures, often involving significant code changes
- Refactoring advantages include optimized cloud benefits, improved scalability, reduced operational costs, and enhanced agility
- Refactoring disadvantages include longer timelines, higher upfront investment, development risks, and requiring new skills
- Replatforming offers middle ground by making targeted optimizations like managed databases while maintaining core application architecture
- Business drivers for lift-and-shift include data center exit deadlines, quick wins, and risk-averse compliance requirements
- Business drivers for refactor include long-term cost optimization, scaling requirements, and competitive advantage through agility
- Hybrid approaches migrate some applications with lift-and-shift while refactoring others based on business value and technical requirements

## Vendor lock-in considerations
- Vendor lock-in occurs when dependency on a specific cloud provider's proprietary services makes switching providers costly or technically difficult
- Technical lock-in arises from using provider-specific APIs, services, or features that have no equivalent in other cloud platforms
- Contractual lock-in results from long-term commitments, volume discounts, or early termination penalties that make switching expensive
- Data lock-in involves challenges in moving large volumes of data between providers due to egress costs, transfer times, and format differences
- Skills lock-in occurs when teams develop expertise in specific provider tools and services that don't transfer to other platforms
- Mitigation strategies include using open standards, multi-cloud approaches, and abstraction layers to reduce dependency on specific providers
- Containerization and Kubernetes provide portable deployment models that reduce lock-in at the application packaging level
- Infrastructure as code enables re-deployment across providers but requires provider-specific configurations for each platform
- Cost analysis should include switching costs when comparing provider offerings and negotiating contracts
- Strategic decisions require balancing lock-in risks against benefits of using advanced provider-specific services that may justify the dependency

## Cloud security fundamentals
- Defense in depth implements multiple layers of security controls so that failure of one layer doesn't compromise overall security
- Identity and access management authenticates users and authorizes their access to resources following least-privilege principles
- Encryption protects data at rest and in transit, ensuring confidentiality even if other controls fail or data is intercepted
- Network security controls including firewalls, segmentation, and web application firewalls protect against network-based attacks
- Security monitoring continuously detects threats, anomalies, and policy violations through logging, analysis, and alerting
- Vulnerability management identifies and remediates security weaknesses in applications, configurations, and infrastructure
- Incident response procedures define how to detect, contain, eradicate, and recover from security incidents
- Compliance validation ensures security controls meet regulatory and policy requirements through continuous assessment
- Shared responsibility clarifies which security aspects customers own versus providers, preventing gaps in coverage
- Security automation enables rapid response to threats and consistent enforcement of security policies across environments

## Identity and access management basics
- Authentication verifies identity through factors including something you know (password), something you have (token), or something you are (biometrics)
- Authorization determines what authenticated identities can access and perform, following least-privilege principles
- Multi-factor authentication requires two or more verification methods, significantly reducing risk of credential compromise
- Single sign-on enables users to authenticate once and access multiple applications without re-entering credentials
- Identity federation establishes trust between identity providers and service providers, enabling external identity usage
- Role-based access control assigns permissions to roles rather than individuals, simplifying management and improving security
- Policy-based access defines fine-grained permissions through policies that specify allowed or denied actions under conditions
- Service identities (service accounts) enable applications and services to authenticate and access resources without human interaction
- Privileged access management controls and monitors highly privileged accounts that could cause significant damage if compromised
- Identity lifecycle management provisions, modifies, and deprovisions access as users join, change roles, or leave organizations

## Cloud networking fundamentals
- Virtual networks provide isolated, software-defined network environments in the cloud, resembling traditional on-premises networks
- Subnets divide virtual networks into smaller segments for organization, security, and traffic management purposes
- IP addressing assigns public and private IP addresses to cloud resources, with private addresses used for internal communication
- Network address translation enables resources with private IPs to access internet while hiding internal addressing from external networks
- DNS resolves domain names to IP addresses, with cloud providers offering managed DNS services for reliability and low latency
- Load balancing distributes traffic across multiple resources for high availability, scalability, and health-aware routing
- Firewalls control traffic based on rules, filtering at the network or application layer to block unauthorized access
- Virtual private networks extend on-premises networks to cloud securely over the internet using encrypted tunnels
- Direct connections provide private, dedicated physical connections between on-premises and cloud for consistent performance
- Content delivery networks cache content at edge locations, reducing latency and bandwidth costs for global users

## Cloud monitoring basics
- Metrics collect numerical data about resource utilization, performance, and health at regular intervals for trend analysis and alerting
- Logs capture detailed records of events, errors, and activities from applications, operating systems, and cloud services
- Traces track requests as they travel through distributed systems, identifying bottlenecks and failures in complex architectures
- Dashboards visualize monitoring data in customizable views, providing at-a-glance status of system health and performance
- Alerts notify operators when metrics exceed thresholds or specific events occur, enabling rapid response to issues
- Health checks continuously verify that resources and applications are responding correctly, triggering remediation when they fail
- Uptime monitoring tracks service availability from multiple locations, detecting outages before users are affected
- Performance monitoring measures response times, throughput, and error rates to identify degradation and capacity needs
- Cost monitoring tracks cloud spending, identifying anomalies and opportunities for optimization
- Compliance monitoring continuously checks configurations against policies, detecting violations and generating audit evidence

## Cloud automation basics
- Infrastructure as code manages and provisions infrastructure through machine-readable definition files rather than manual processes
- Configuration management tools ensure systems maintain desired state, automatically correcting drift from defined configurations
- Orchestration coordinates multiple automated tasks into workflows, managing dependencies and execution order
- Continuous integration automatically builds and tests code changes, providing rapid feedback on quality and integration issues
- Continuous deployment automates release of validated code to environments, reducing manual effort and errors
- Policy as code defines governance rules programmatically, enabling automated enforcement across cloud resources
- Auto-scaling automatically adjusts resource capacity based on demand, optimizing cost and performance without manual intervention
- Event-driven automation triggers actions in response to events, enabling self-healing and automated remediation
- Serverless automation runs code in response to events without managing servers, simplifying automation implementation
- API-driven automation enables programmatic control of cloud resources, integrating with existing tools and workflows

# Cloud Architecture

## Cloud architecture principles
- Design for failure assumes components will fail and builds systems that remain available despite individual failures
- Loose coupling minimizes dependencies between components, allowing them to evolve independently and failures to be contained
- Elasticity enables systems to scale resources dynamically based on demand, optimizing cost and performance
- Automation reduces manual intervention, minimizing human error and enabling consistent, repeatable operations
- Security by design incorporates security throughout architecture rather than adding as an afterthought
- Data management strategies address data partitioning, replication, consistency, and lifecycle for distributed systems
- Cost optimization balances performance and features against expenses, eliminating waste and maximizing business value
- Performance efficiency ensures systems meet response time and throughput requirements under expected loads
- Operational excellence focuses on running and monitoring systems effectively, with observability and incident response
- Sustainability considers environmental impact through efficient resource utilization and workload placement

## Well-architected frameworks
- AWS Well-Architected Framework provides six pillars: Operational Excellence, Security, Reliability, Performance Efficiency, Cost Optimization, and Sustainability
- Operational Excellence pillar focuses on running and monitoring systems, continuous improvement, and automated operations
- Security pillar addresses data protection, identity management, infrastructure protection, and incident detection and response
- Reliability pillar ensures systems recover from failures, scale dynamically, and meet availability and recovery objectives
- Performance Efficiency pillar maintains efficiency as demand changes, using monitoring and optimization to evolve systems
- Cost Optimization pillar avoids unnecessary costs, matches supply with demand, and increases expenditure awareness over time
- Sustainability pillar minimizes environmental impact through efficient resource utilization and workload optimization
- Microsoft Azure Well-Architected Framework includes five pillars: Cost Optimization, Operational Excellence, Performance Efficiency, Reliability, and Security
- Google Cloud Architecture Framework organizes best practices across system design, security, privacy, reliability, cost optimization, and performance
- Review questions in each pillar guide architects through evaluating designs against best practices and identifying improvements

## Scalability patterns
- Horizontal scaling (scale out) adds more instances of a resource, distributing load across multiple servers for increased capacity
- Vertical scaling (scale up) increases capacity of existing instances by adding more CPU, memory, or other resources
- Stateless design ensures any instance can handle any request by storing session state in external shared storage
- Database read replicas distribute read queries across multiple copies, increasing overall query throughput
- Sharding partitions data across multiple databases based on a shard key, distributing both read and write load
- Queue-based load leveling uses message queues to smooth request spikes, allowing consumers to process at optimal rate
- Auto-scaling automatically adjusts resources based on demand, using metrics, schedules, or predictive algorithms
- Content delivery networks cache static and dynamic content at edge locations, reducing load on origin servers
- Caching stores frequently accessed data in fast memory, reducing database load and improving response times
- Asynchronous processing offloads long-running tasks to background processes, keeping frontends responsive under load

## Resilience patterns
- Retry with exponential backoff automatically retries failed operations with increasing delays, preventing overwhelming recovering services
- Circuit breaker prevents repeated calls to failing services, allowing time for recovery and failing fast when appropriate
- Bulkhead isolates failures by partitioning resources into separate pools, preventing failure in one area from cascading
- Health endpoint monitoring exposes service health through dedicated endpoints, enabling load balancers and orchestrators to route traffic appropriately
- Throttling limits request rates to protect services from overload, maintaining availability for all users
- Graceful degradation reduces functionality rather than completely failing, maintaining core user experience during partial outages
- Fallback provides alternative responses or functionality when primary services fail, maintaining some level of service
- Leader election ensures only one instance performs specific tasks, preventing conflicts and ensuring reliable execution
- Quorum-based decisions require consensus among distributed components, ensuring consistency despite partial failures
- Chaos engineering proactively introduces failures to test resilience, revealing weaknesses before they cause real incidents

## Reliability engineering
- Service Level Objectives define target reliability levels that guide design decisions and operational practices
- Error budgets calculate acceptable failure rates as difference between perfect reliability and SLO, balancing innovation against stability
- Redundancy eliminates single points of failure through multiple instances, availability zones, and regions
- Fault isolation prevents failures from spreading by containing them within boundaries like cells or shards
- Automated recovery responds to failures without human intervention, reducing downtime and eliminating human error
- Capacity planning ensures resources meet demand through monitoring trends, modeling growth, and planning for spikes
- Dependency management identifies and mitigates risks from external dependencies through fallbacks and graceful degradation
- Incident management processes detect, respond to, and learn from failures through blameless post-mortems
- Load testing validates system behavior under expected and peak loads, identifying bottlenecks before production
- Release engineering ensures safe deployments through canary releases, blue-green deployments, and feature flags

## Distributed systems fundamentals
- Distributed systems consist of multiple independent computers appearing to users as a single coherent system
- Network communication introduces latency, partial failure, and security considerations not present in single systems
- Consensus algorithms enable distributed components to agree on values despite failures (Paxos, Raft)
- Time and ordering challenges arise from lack of global clock, requiring logical clocks for event ordering
- Distributed transactions coordinate operations across multiple systems, with trade-offs between consistency and availability
- Message passing enables communication between components through synchronous (request-response) or asynchronous patterns
- Distributed coordination services (ZooKeeper, etcd) manage configuration, synchronization, and group services
- Distributed caching improves performance by storing data closer to computation, with consistency challenges
- Distributed monitoring aggregates metrics, logs, and traces from multiple components for observability
- Failure detection identifies failed nodes through timeouts, heartbeats, and gossip protocols

## CAP theorem
- CAP theorem states distributed data stores can provide at most two of three guarantees: Consistency, Availability, and Partition tolerance
- Consistency means all nodes see the same data simultaneously, with reads returning most recent write
- Availability means every request receives a response, without guarantee it contains the most recent write
- Partition tolerance means system continues operating despite network partitions dropping or delaying messages
- During network partition, systems must choose between consistency (refusing writes until partition heals) or availability (accepting writes with potential inconsistency)
- CP systems prioritize consistency over availability during partitions, blocking writes until consistency can be ensured
- AP systems prioritize availability over consistency during partitions, accepting writes that may cause temporary inconsistency
- PACELC extends CAP, stating in absence of partition, systems choose between latency and consistency
- Real-world systems often provide tunable consistency, allowing different guarantees for different operations
- Understanding CAP guides database selection and architecture design based on application requirements

## Consistency models
- Strong consistency ensures all reads see the most recent write, providing a linearizable view of data
- Eventual consistency guarantees that if no new updates, all replicas will eventually converge to the same value
- Read-after-write consistency ensures a client always sees its own writes immediately after writing
- Monotonic read consistency guarantees a client sees increasingly recent data over time, never going back in time
- Monotonic write consistency ensures writes from a client are applied in the order they were issued
- Causal consistency ensures causally related operations appear in the same order to all nodes
- Quorum-based consistency uses configurable read and write quorums to balance consistency and performance
- Session consistency provides consistency guarantees within a client session, simplifying application development
- Transactional consistency (ACID) ensures atomic, consistent, isolated, and durable operations
- BASE (Basically Available, Soft state, Eventual consistency) contrasts with ACID for distributed systems

## Event-driven architecture
- Event-driven architecture uses events to trigger and communicate between decoupled services
- Event producers generate events without knowledge of consumers, enabling loose coupling and scalability
- Event consumers react to events asynchronously, processing them when resources are available
- Event routers or brokers (Kafka, EventBridge, SNS) receive events and deliver them to appropriate consumers
- Event sourcing stores state changes as sequence of events, enabling auditability and state reconstruction
- Command Query Responsibility Segregation separates write operations (commands) from read operations (queries)
- Eventual consistency accepts temporary inconsistency between event publication and consumer processing
- Dead letter queues capture events that cannot be processed, enabling debugging and reprocessing
- Schema evolution manages changes to event structures while maintaining backward compatibility
- Idempotent event handling ensures processing an event multiple times has same effect as processing once

## Microservices architecture
- Microservices architecture structures applications as collections of small, independently deployable services
- Services are organized around business capabilities, with each service owning specific domain functionality
- Independent deployability allows services to be updated without redeploying entire application
- Decentralized data management gives each service its own database, avoiding shared schemas
- Service boundaries defined by bounded contexts from domain-driven design ensure proper separation
- API gateways provide single entry point, handling cross-cutting concerns like authentication and routing
- Service discovery enables services to locate each other dynamically in changing environments
- Inter-service communication occurs through lightweight protocols like HTTP/REST or messaging
- Resilience patterns (circuit breakers, retries) handle partial failures without cascading
- Observability through distributed tracing, metrics aggregation, and centralized logging

## Serverless architecture
- Serverless architecture allows building applications without managing servers, with cloud providers handling infrastructure
- Function-as-a-Service (FaaS) executes code in response to events, scaling automatically and charging per invocation
- Backend-as-a-Service (BaaS) provides managed backend services like databases, authentication, and storage
- Event-driven design triggers functions in response to events from various sources (HTTP requests, file uploads, queue messages)
- Cold starts occur when functions are invoked after inactivity, adding latency while initializing
- Stateless functions cannot rely on local memory for persistence, requiring external storage for state
- Granular scaling scales individual functions independently based on their specific invocation patterns
- Pay-per-execution pricing charges only for compute time used during invocations, with no cost for idle time
- Vendor lock-in concerns arise from using provider-specific function interfaces and event sources
- Composition patterns combine multiple functions into workflows using orchestration services like Step Functions

## Container-based architecture
- Container-based architecture packages applications with dependencies into lightweight, portable units
- Containers share host operating system kernel, providing efficiency compared to virtual machines
- Images define container contents, built from layered filesystems for efficient storage and distribution
- Container orchestration (Kubernetes, ECS, AKS) manages deployment, scaling, and networking of containers
- Pods (in Kubernetes) group related containers sharing network namespace and storage volumes
- Service mesh (Istio, Linkerd) provides networking features like service discovery, load balancing, and security
- Immutable infrastructure treats containers as disposable, replacing rather than patching running containers
- Configuration management separates configuration from container images using environment variables or mounted volumes
- Container registries store and distribute container images, with public (Docker Hub) and private options
- Resource limits and requests ensure containers receive necessary resources without starving others

## Monolithic vs microservices
- Monolithic architecture builds application as single unified unit with all components tightly integrated
- Monolith advantages include simpler development, testing, and deployment with fewer distributed system concerns
- Monolith disadvantages include scaling entire application together, technology lock-in, and complex maintenance as codebase grows
- Microservices architecture splits application into independent services with clear boundaries and responsibilities
- Microservices advantages include independent scaling, technology diversity, and autonomous team ownership
- Microservices disadvantages include distributed system complexity, network latency, and operational overhead
- Modular monolith offers compromise with well-defined modules within single deployment unit
- Strangler pattern gradually replaces monolith pieces with microservices over time
- Deployment frequency often increases with microservices, enabling faster feature delivery
- Organizational structure influences architecture choice (Conway's Law), with microservices suiting autonomous teams

## API-first architecture
- API-first architecture prioritizes API design before implementation, treating APIs as first-class products
- Contract-first development defines API specifications (OpenAPI, GraphQL schema) before writing code
- Design for consumers creates APIs focused on consumer needs rather than implementation details
- Consistent API design applies standards across all APIs for naming, error handling, and versioning
- API versioning manages changes through URI versioning, content negotiation, or semantic versioning
- API documentation generated from specifications ensures accuracy and provides interactive exploration
- API gateways provide unified entry point for authentication, rate limiting, monitoring, and routing
- RESTful design uses HTTP methods and resource-oriented URLs for intuitive, standards-based APIs
- GraphQL offers flexible querying, allowing clients to request exactly the data they need
- gRPC provides high-performance RPC with strong typing and efficient binary serialization

## Domain-driven design
- Domain-driven design focuses on modeling complex business domains in software through collaboration between domain experts and developers
- Ubiquitous language establishes common vocabulary between technical and domain experts used consistently in code and discussions
- Bounded contexts define explicit boundaries within which particular domain models apply, preventing model confusion
- Entities are objects with identity that remains consistent throughout their lifecycle, regardless of attribute changes
- Value objects are immutable objects defined by their attributes, with no conceptual identity
- Aggregates cluster related entities and value objects treated as single unit for data changes
- Repositories provide collection-like access to aggregates, abstracting data storage details
- Domain services encapsulate domain logic that doesn't naturally fit within entities or value objects
- Domain events capture occurrences within the domain that domain experts care about
- Context mapping identifies relationships between bounded contexts, guiding integration strategies

## Load balancing patterns
- Round-robin distributes requests sequentially across servers, simplest algorithm but doesn't account for server load
- Least connections routes to servers with fewest active connections, effective for varying request processing times
- Least response time routes to servers with fastest response times, improving user experience
- IP hash routes requests from same client IP to same server, maintaining session affinity when needed
- Weighted distribution assigns different weights to servers based on capacity, sending proportionally more traffic to powerful servers
- Layer 4 load balancing operates at transport layer, routing based on IP and port with minimal processing
- Layer 7 load balancing operates at application layer, making routing decisions based on HTTP headers, cookies, or content
- Global server load balancing distributes traffic across geographically distributed data centers
- Internal load balancing distributes traffic within application tiers, such as between web and application servers
- Health checks monitor server status, removing unhealthy servers from rotation automatically

## Caching strategies
- Cache-aside (lazy loading) checks cache first, loads from database on miss and stores in cache for subsequent requests
- Read-through caching automatically loads data from database into cache when cache miss occurs
- Write-through caching writes data to cache and database simultaneously in same transaction
- Write-behind (write-back) caching writes to cache immediately and asynchronously updates database later
- Cache eviction policies determine which items to remove when cache fills (LRU, LFU, FIFO, TTL)
- Distributed caching shares cache across multiple servers, providing larger capacity and high availability
- Content delivery networks cache static and dynamic content at edge locations close to users
- Database query caching stores result sets of frequent queries, reducing database load
- Object caching stores serialized objects, avoiding reconstruction overhead for complex data
- Cache invalidation removes or updates cached data when source data changes, ensuring consistency

## Data partitioning
- Horizontal partitioning (sharding) distributes rows across multiple databases based on shard key
- Vertical partitioning splits tables by columns, separating frequently accessed columns from less used ones
- Functional partitioning separates data by business function or domain into different databases
- Directory-based partitioning maintains lookup service mapping data to partitions, enabling flexible sharding
- Range-based partitioning assigns data to shards based on ranges of partition key values
- Hash-based partitioning applies hash function to partition key to determine shard, distributing data evenly
- Consistent hashing minimizes remapping when adding or removing shards, only affecting neighboring shards
- Partition key selection critically impacts performance, with poor keys causing hotspots or uneven distribution
- Cross-partition queries become complex, often requiring scatter-gather approach querying all partitions
- Data redistribution (resharding) becomes necessary as data grows, requiring careful planning to minimize disruption

## Replication strategies
- Single leader replication designates one node as primary for writes, with replicas asynchronously or synchronously updating
- Multi-leader replication allows multiple nodes to accept writes, synchronizing changes between them
- Leaderless replication (Dynamo-style) enables writes to any node, with read repair and anti-entropy for consistency
- Synchronous replication ensures write is committed to leader and all replicas before acknowledging success
- Asynchronous replication acknowledges write after leader commits, with replicas updated eventually
- Read replicas serve read traffic, offloading queries from primary and improving read throughput
- Cross-region replication copies data to different geographic regions for disaster recovery and local access
- Snapshot replication takes point-in-time copies for backup, reporting, or initializing new replicas
- Transactional replication propagates individual changes as they occur, maintaining near real-time consistency
- Merge replication allows changes at multiple locations to be combined, with conflict resolution handling inconsistencies

## Stateless architecture
- Stateless architecture stores no session data on servers, with each request containing all necessary information
- External session storage moves state to shared data stores like Redis or databases accessible from any instance
- Client-side state management stores session data in client (cookies, local storage) sent with each request
- Token-based authentication (JWT) includes user identity and claims in self-contained tokens verified by servers
- Idempotent operations can be repeated safely, critical for stateless systems with potential retries
- Horizontal scaling becomes simpler since any server can handle any request without session affinity
- Failover improves because requests can route to any healthy instance without losing session context
- Cache-friendly architecture leverages distributed caches that any instance can access
- RESTful principles align with statelessness, with each request containing all needed context
- Reduced server complexity eliminates session management code and memory overhead

## Stateful architecture
- Stateful architecture maintains session or user-specific data on servers between requests
- Session affinity (sticky sessions) routes requests from same client to same server maintaining local state
- In-memory state stores session data in server memory for fastest access but risks data loss on failure
- Distributed state stores across multiple servers, enabling failover while maintaining state accessibility
- Stateful services maintain conversational state across multiple client interactions
- WebSocket connections maintain persistent bidirectional communication with stateful connection context
- Database sessions maintain transaction state across multiple operations before commit or rollback
- Stateful stream processing maintains operator state (aggregations, windows) across stream events
- State replication synchronizes state across instances for high availability
- State checkpointing periodically persists state to durable storage for recovery after failures

## Edge computing
- Edge computing processes data near its source rather than in centralized cloud data centers
- Reduced latency enables real-time applications requiring single-digit millisecond responses
- Bandwidth optimization processes data locally, transmitting only relevant results to cloud
- Local data processing addresses data sovereignty requirements by keeping data within geographic boundaries
- Edge devices include IoT sensors, cameras, gateways, and local servers with compute capabilities
- Cloud edge extends cloud services to edge locations (AWS Outposts, Azure Stack, Google Distributed Cloud)
- 5G integration combines edge computing with high-speed, low-latency cellular networks
- Content delivery networks evolve to support compute at edge, not just content caching
- Autonomous operations continue functioning during network disconnection, syncing when connectivity restored
- Security considerations include physical protection of distributed devices and secure communication

## Global architecture design
- Global architecture spans multiple geographic regions to serve worldwide users with low latency
- Latency-based routing directs users to nearest region based on network distance and performance
- Geographically distributed data must address consistency, replication, and sovereignty requirements
- Active-active global deployments run workloads simultaneously in multiple regions
- Active-passive global deployments direct traffic to primary region, with others on standby for failover
- Cross-region replication synchronizes data between regions for disaster recovery and local access
- Global load balancing distributes traffic across regions using DNS-based or anycast routing
- Content delivery networks cache content at global edge locations, reducing origin load and latency
- Compliance with data residency laws may require keeping certain data within specific geographic boundaries
- Global scale requires handling diverse regulations, languages, currencies, and cultural considerations

## Multi-region design
- Multi-region design deploys applications across multiple cloud regions for resilience and performance
- Active-active multi-region runs production workloads simultaneously across regions
- Active-passive multi-region keeps standby region ready for failover without serving production traffic
- Data replication strategies must balance consistency, latency, and cost across regions
- Global traffic management routes users to appropriate region based on latency, health, or location
- Disaster recovery benefits include ability to survive region-wide outages with minimal disruption
- Compliance requirements may necessitate specific region selection for data residency
- Increased complexity in deployment, monitoring, and operations requires automation and orchestration
- Cost implications include data transfer between regions and running redundant infrastructure
- Consistency challenges require careful design for applications requiring strong global consistency

## Failover strategies
- Active-passive failover maintains standby infrastructure that activates when primary fails
- Active-active failover distributes load across multiple regions, with others absorbing traffic when one fails
- Automatic failover detects failures and redirects traffic without human intervention
- Manual failover requires human decision and action, providing control during complex incidents
- Database failover promotes replica to primary when primary database becomes unavailable
- DNS failover updates DNS records to redirect traffic during regional failures
- Application-level failover redirects requests within application logic based on service health
- State synchronization ensures failover target has necessary state to continue operations
- Failback procedures return operations to original primary after recovery
- Failover testing validates procedures through regular drills without impacting production

## Observability architecture
- Observability enables understanding system internal state through external outputs (metrics, logs, traces)
- Metrics provide numerical measurements over time, aggregatable for trend analysis and alerting
- Logs capture discrete events with detailed context, essential for debugging and auditing
- Traces follow requests through distributed systems, identifying bottlenecks and failures
- Correlation IDs connect related events across services, enabling end-to-end request tracking
- Distributed tracing systems (Jaeger, Zipkin) collect and visualize trace data across service boundaries
- Metrics aggregation (Prometheus, CloudWatch) collects, stores, and queries time-series data
- Log aggregation (ELK stack, Loki) centralizes logs for searching and analysis
- Alerting based on observability data notifies operators of anomalies and threshold violations
- Dashboards visualize observability data, providing at-a-glance system health understanding

## Logging and tracing design
- Structured logging outputs logs in consistent, parseable format (JSON) enabling automated analysis
- Log levels (DEBUG, INFO, WARN, ERROR) categorize log importance for filtering and alerting
- Centralized log aggregation collects logs from all services into searchable repository
- Log retention policies balance storage costs against compliance and debugging requirements
- Sensitive data masking prevents logging passwords, PII, and other confidential information
- Distributed tracing correlates operations across service boundaries using trace IDs
- Sampling strategies reduce tracing overhead by recording only subset of requests
- Context propagation carries trace context through asynchronous operations and message queues
- Span attributes provide detailed context about each operation within trace
- Trace visualization shows request flow, timing, and errors across distributed system

## Security architecture patterns
- Defense in depth layers security controls throughout architecture, preventing single point of failure
- Zero trust architecture assumes no implicit trust, verifying every access attempt regardless of source
- Perimeter security protects network boundaries with firewalls, VPNs, and DDoS protection
- Identity and access management centralizes authentication and authorization across services
- Encryption at rest protects stored data using envelope encryption with key management
- Encryption in transit protects data moving between services using TLS/SSL
- Secrets management securely stores and rotates credentials, API keys, and certificates
- Web application firewalls protect against application-layer attacks like SQL injection and XSS
- Security information and event management (SIEM) aggregates and analyzes security logs
- Compliance automation continuously validates configurations against security policies

## Zero trust architecture
- Zero trust architecture assumes no implicit trust based on network location, verifying every access request
- Verify explicitly always authenticates and authorizes based on all available data points
- Least privilege access limits user and service permissions to minimum necessary for function
- Assume breach designs systems assuming attacker presence, limiting blast radius and enabling detection
- Micro-segmentation divides network into small zones with separate access requirements
- Identity-based access control uses identity as primary security perimeter rather than network
- Continuous monitoring and validation checks behavior throughout sessions, not just at entry
- Device health compliance ensures only trusted devices can access resources
- Data-centric protection focuses on securing data itself regardless of location
- Automation of security policies ensures consistent enforcement across dynamic environments

## Cost-optimized architecture
- Rightsizing matches resource capacity to actual requirements, avoiding over-provisioning
- Auto-scaling adjusts resources dynamically based on demand, eliminating idle capacity costs
- Reserved instances and savings plans provide discounts for committed usage of predictable workloads
- Spot instances run fault-tolerant workloads on spare capacity at significant discounts
- Storage tiering moves data to appropriate classes based on access patterns (hot, cool, archive)
- Data lifecycle policies automatically delete or transition data to reduce storage costs
- Resource tagging enables cost allocation and chargeback to business units
- Monitoring and alerting detects cost anomalies and optimization opportunities
- Serverless architectures eliminate idle compute costs by paying only for actual usage
- Architecture reviews regularly identify and eliminate waste through continuous optimization

## Performance optimization patterns
- Caching stores frequently accessed data in fast memory, reducing latency and backend load
- Database indexing optimizes query performance by providing fast data lookup paths
- Connection pooling reuses database connections, avoiding overhead of establishing new connections
- Asynchronous processing offloads slow operations from request path, improving responsiveness
- Compression reduces data size for transfer and storage, improving network performance
- CDN caching brings content closer to users, reducing latency and origin load
- Content optimization minimizes payload size through minification, image optimization, and lazy loading
- Query optimization analyzes and improves slow database queries through indexing and rewriting
- Compute optimization selects appropriate instance types and sizes for workload characteristics
- Performance testing continuously measures and validates system behavior under various loads

# AWS

## Core AWS Concepts

### AWS global infrastructure
- AWS global infrastructure spans 33 geographic regions and 105 availability zones as of 2024, with continued expansion planned
- Each region consists of multiple, isolated Availability Zones connected through low-latency, high-bandwidth fiber optic networking
- Availability Zones are physically separated by meaningful distance (many kilometers) but within 100 km to enable synchronous replication
- Edge locations form a global network of over 600+ points of presence for Amazon CloudFront content delivery
- Regional edge caches sit between origin servers and edge locations, improving cache hit ratios for less frequently accessed content
- AWS GovCloud regions provide isolated infrastructure meeting specific US government compliance requirements
- China regions operate through joint venture partners (Beijing Sinnet, Ningxia NWCD) meeting Chinese regulatory requirements
- Local Zones extend infrastructure to metropolitan areas for ultra-low latency applications requiring single-digit millisecond response
- Wavelength Zones embed AWS compute at telecommunications providers' 5G network edges
- Outposts bring AWS infrastructure and services on-premises for hybrid workloads requiring low latency to on-premises systems

### Regions and availability zones
- Region selection criteria include data residency requirements, latency to users, service availability, and cost considerations
- Each region operates independently with no automatic cross-region replication unless explicitly configured by customers
- Availability Zones within a region are interconnected with high-bandwidth, low-latency networking for synchronous replication
- Multi-AZ deployments protect against data center failures by distributing resources across isolated Availability Zones
- AZ failure impacts are contained within that zone, with other zones in the same region continuing normal operation
- AZ identification differs across accounts to prevent resource concentration in single physical data center
- Local Zones provide single-AZ infrastructure in metropolitan areas, not offering multi-AZ resilience within that location
- Region pairs (in some providers) support disaster recovery with automatic replication for certain services
- Geographic compliance requires understanding where data resides physically and which regions meet regulatory requirements
- Service availability varies by region, with newer services often launching first in major regions before expanding

### AWS shared responsibility model
- AWS is responsible for security OF the cloud, including physical infrastructure, hardware, software, and facilities globally
- AWS responsibility includes protecting global infrastructure across regions, availability zones, and edge locations
- AWS manages virtualization layer, isolating customer workloads from each other at the hypervisor level
- Customers are responsible for security IN the cloud, including data, identity, access, and configurations
- Customer responsibility covers guest operating systems, applications, firewall rules, and network configurations
- For abstracted services (Lambda, RDS), AWS takes greater responsibility for underlying platform security
- Configuration of AWS services (S3 bucket policies, security groups) falls under customer responsibility
- Compliance responsibilities are shared, with AWS providing certified infrastructure and customers ensuring compliant use
- Data classification and encryption decisions remain entirely customer responsibility regardless of service model
- Shared responsibility varies by service type, with more responsibility shifted to AWS in managed services

### AWS Well-Architected Framework
- Operational Excellence pillar focuses on running and monitoring systems, continuous improvement, and automated operations
- Key operational excellence practices include infrastructure as code, small frequent changes, and learning from failures
- Security pillar emphasizes identity management, detective controls, infrastructure protection, and data protection
- Security practices include least privilege access, encryption everywhere, and automated security validation
- Reliability pillar ensures systems recover from failures, scale dynamically, and meet availability objectives
- Reliability practices include automatic failover, recovery testing, and capacity planning
- Performance Efficiency pillar maintains efficiency as demand changes through experimentation and monitoring
- Performance practices include serverless architectures, multi-region deployment, and performance review
- Cost Optimization pillar avoids unnecessary costs through careful resource selection and continuous monitoring
- Sustainability pillar (added 2021) focuses on minimizing environmental impact through efficient design and workload optimization

### AWS pricing models
- On-Demand pricing charges for compute or database capacity by hour or second with no long-term commitments
- Reserved Instances provide significant discounts (up to 72%) in exchange for one or three-year commitment
- Standard RIs offer highest discount but require committing to specific instance family in specific region
- Convertible RIs offer lower discount but allow changing instance attributes during the term
- Scheduled RIs reserve capacity for specific time windows, ideal for predictable recurring workloads
- Savings Plans offer flexibility across instance families while providing discounts similar to RIs
- Compute Savings Plans apply to any EC2 instance regardless of family, region, or OS
- EC2 Instance Savings Plans apply to specific instance family in chosen region
- Spot Instances provide up to 90% discounts for interruptible workloads with two-minute termination warning
- Data transfer pricing includes costs for data leaving AWS regions, while ingress is typically free

### AWS management console
- AWS Management Console provides web-based graphical interface for managing all AWS services
- Console homepage offers personalized dashboards showing recent activity, cost explorer, and service health
- AWS Console Mobile Application enables monitoring resources and responding to issues from mobile devices
- Console-to-Code feature records console actions as infrastructure as code (CloudFormation, CDK) for automation
- AWS Systems Manager provides unified interface for operational data and automation across AWS resources
- AWS Pricing Calculator helps estimate costs before deployment directly from console
- Service health dashboards display real-time status of all AWS services across regions
- AWS Console supports multiple identities (root user, IAM users) with appropriate permissions
- Saved views allow customizing console layout for frequently accessed services and resources
- AWS Console integrates with AWS Organizations for cross-account management through single sign-on

### AWS CLI and SDKs
- AWS Command Line Interface (CLI) provides unified tool for managing AWS services from command line
- CLI supports multiple profiles for managing different accounts and regions from single installation
- AWS SDKs available for multiple languages including Python (boto3), JavaScript, Java, .NET, Go, Ruby, PHP
- SDKs handle authentication, request signing, retries, and error handling automatically
- AWS CLI v2 includes features like AWS Single Sign-On integration and interactive workflows
- AWS Tools for PowerShell provides cmdlets for managing AWS resources from PowerShell
- AWS CloudShell provides browser-based shell with pre-authenticated AWS CLI in the console
- AWS CDK uses SDKs to generate CloudFormation templates from high-level programming languages
- AWS SDKs support pagination for handling large result sets automatically
- SDK credential chain automatically searches multiple locations (environment variables, ~/.aws, instance metadata)

### IAM fundamentals
- IAM (Identity and Access Management) centrally manages access to AWS services and resources
- Users represent individual people or applications with permanent credentials for API access
- Groups organize users with shared permissions, simplifying management through group-level policies
- Roles provide temporary credentials for entities that need to assume permissions without permanent keys
- Policies are JSON documents defining permissions, attached to identities or resources
- Managed policies are standalone policies reusable across multiple identities
- Inline policies are embedded directly within a single user, group, or role
- Permission boundaries set maximum permissions an identity can receive, even with explicit grants
- Service control policies (in Organizations) centrally control maximum permissions across accounts
- IAM Access Analyzer identifies resources shared with external entities, helping validate least privilege

### AWS organizations
- AWS Organizations centrally govern multiple AWS accounts under single umbrella
- Consolidated billing aggregates charges from all accounts, enabling volume discounts and single payment
- Service control policies (SCPs) centrally restrict maximum permissions across member accounts
- Organizational units (OUs) group accounts for hierarchical policy application
- Account creation automation enables programmatic creation of new accounts with baseline configurations
- Tag policies enforce consistent tagging across accounts for cost allocation and governance
- Backup policies automatically configure backup plans across accounts
- AI services opt-out policies control whether data from accounts can be used for AWS service improvement
- Multi-account strategy isolates workloads, environments, and teams while enabling centralized governance
- AWS Organizations integrates with IAM for cross-account access through role assumption

### Resource tagging
- Resource tagging attaches metadata key-value pairs to AWS resources for organization and automation
- Cost allocation tags enable detailed cost tracking and chargeback to business units or projects
- AWS-generated tags are automatically created by AWS (e.g., aws:createdBy)
- User-defined tags are custom tags created by customers for their specific needs
- Tag policies enforce consistent tagging rules across organization, including allowed values and requirements
- Resource groups collect resources with same tags for unified management and automation
- Tag-based access control uses tags in IAM policies to control access to resources
- Automation targets use tags to identify resources for backup, patching, or shutdown schedules
- Tag editor provides central interface for adding, removing, and managing tags across resources
- Tagging best practices include standardized naming conventions, mandatory tags (Owner, Environment, Cost Center)

## Compute

### EC2 architecture
- EC2 (Elastic Compute Cloud) provides resizable virtual machines in the cloud with complete control over configuration
- Amazon Machine Images (AMIs) are pre-configured templates containing operating system and software
- Instance types define CPU, memory, storage, and networking capacity with families optimized for different workloads
- Instance lifecycle includes pending, running, stopping, stopped, and terminated states
- EC2 uses Xen and Nitro hypervisors for virtualization, with Nitro providing near-bare-metal performance
- Nitro system offloads virtualization functions to dedicated hardware, reducing overhead
- Instance store volumes provide temporary, high-performance storage that persists only during instance lifetime
- Elastic Network Interfaces (ENIs) provide network connectivity with attributes that persist across instance starts/stops
- User data scripts run at instance launch for bootstrap configuration
- Instance metadata service provides instance-specific information (IP addresses, tags) from within instance

### EC2 instance types
- General purpose instances (T3, M5, M6g) balance compute, memory, and networking for diverse workloads
- Compute optimized instances (C5, C6g) provide high-performance processors for compute-intensive applications
- Memory optimized instances (R5, X1, z1d) offer large memory sizes for in-memory databases and caches
- Storage optimized instances (I3, D2, H1) provide high, sequential read/write access to large datasets
- Accelerated computing instances (P3, P4, G4) include GPUs or FPGAs for machine learning and graphics
- Burstable instances (T series) accumulate CPU credits during idle periods for use during peak demand
- Dedicated hosts provide physical servers dedicated for customer use, enabling existing server-bound software licenses
- Bare metal instances (i3.metal) provide direct access to physical hardware for workloads requiring non-virtualized environment
- Instance size naming (nano, micro, small, medium, large, xlarge) indicates relative capacity within family
- Graviton processors (AWS-designed ARM) offer price-performance benefits for many workloads

### Auto scaling groups
- Auto Scaling groups manage collection of EC2 instances as logical unit for scaling and management
- Launch templates specify instance configuration including AMI, instance type, key pair, and security groups
- Minimum, maximum, and desired capacity define scaling boundaries and target fleet size
- Scaling policies trigger based on CloudWatch metrics (CPU utilization, request count) to add/remove instances
- Scheduled scaling adjusts capacity at specific times for predictable demand patterns
- Predictive scaling uses machine learning to forecast traffic and proactively scale
- Health checks replace unhealthy instances by terminating and launching new ones
- Availability Zone rebalancing maintains balanced distribution across zones after failures
- Lifecycle hooks enable custom actions during instance launch or termination
- Standby state removes instances from service for maintenance without terminating

### Load balancers
- Application Load Balancer (ALB) operates at Layer 7, routing based on HTTP/HTTPS content
- ALB supports path-based routing, host-based routing, and containerized applications
- Network Load Balancer (NLB) operates at Layer 4, handling millions of requests per second with ultra-low latency
- NLB preserves source IP addresses for backend targets and supports static/elastic IPs
- Gateway Load Balancer (GWLB) deploys, scales, and manages third-party virtual appliances
- Classic Load Balancer (CLB) is legacy offering for Layer 4/7 load balancing with limited features
- Target groups route requests to registered targets (EC2 instances, Lambda functions, IP addresses)
- Stickiness (session affinity) routes requests from same client to same target using cookies
- Cross-zone load balancing distributes traffic evenly across all registered instances in all AZs
- Connection draining completes in-flight requests before deregistering instances

### Lambda architecture
- AWS Lambda runs code without provisioning or managing servers, executing in response to events
- Lambda functions are triggered by over 200 AWS services including S3, DynamoDB, API Gateway
- Execution environment includes runtime (Node.js, Python, Java, Go, etc.), memory (128MB-10GB), and ephemeral storage
- Cold starts occur when new execution environments initialize, adding latency to first invocation
- Concurrency limits control maximum simultaneous executions per function or account
- Reserved concurrency guarantees capacity for critical functions
- Provisioned concurrency keeps specified number of environments warm for predictable performance
- Lambda layers package libraries and dependencies separately from function code for reuse
- Lambda extensions integrate monitoring, security, and observability tools into execution environment
- Function URLs provide dedicated HTTPS endpoints for Lambda functions without API Gateway

### Serverless design
- Serverless applications compose managed services without server management
- Event-driven architecture uses events to trigger functions and integrate services
- Function granularity balances single-purpose functions against operational overhead
- Idempotent design ensures processing same event multiple times has same effect
- Error handling includes dead-letter queues and destination configuration for failed events
- Async invocation separates request from processing for long-running operations
- Step Functions orchestrate multiple Lambda functions into workflows with error handling
- API Gateway provides REST or WebSocket APIs fronting Lambda functions
- DynamoDB serves as scalable, serverless database for Lambda applications
- S3 event notifications trigger functions on object creation, deletion, or modification

### Elastic Beanstalk
- Elastic Beanstalk is PaaS offering automatically handling deployment, scaling, and health monitoring
- Platform-as-a-Service abstracts infrastructure while allowing some control through configuration
- Supported platforms include Java, .NET, PHP, Node.js, Python, Ruby, Go, and Docker
- Environment tiers include web server (handling HTTP requests) and worker (processing background tasks)
- Deployment policies include all-at-once, rolling, rolling with batch, immutable, and blue/green
- Managed updates automatically apply platform updates with configurable maintenance windows
- Beanstalk integrates with CloudWatch for monitoring and SNS for notifications
- Environment cloning creates copies for testing or staging
- Configuration files (.ebextensions) customize environment beyond default settings
- Saved configurations enable reusing environment settings across multiple environments

### Containers on AWS
- Amazon ECS (Elastic Container Service) is AWS-native container orchestration service
- Amazon EKS (Elastic Kubernetes Service) provides managed Kubernetes control plane
- AWS Fargate runs containers without managing underlying servers (serverless containers)
- Amazon ECR (Elastic Container Registry) stores and manages container images
- Task definitions (ECS) describe container configurations including images, resources, and networking
- Pods (EKS) group containers that share networking and storage resources
- Service mesh (App Mesh) provides observability, traffic control, and security for microservices
- AWS Copilot simplifies building and deploying containerized applications on ECS/Fargate
- Bottlerocket is AWS-built Linux-based OS optimized for running containers
- FireLens integrates container logs with destinations like CloudWatch and Splunk

### ECS
- ECS (Elastic Container Service) orchestrates Docker containers across EC2 instances or Fargate
- Cluster groups container instances and provides management scope
- Task definition JSON describes one or more containers with resources, networking, and volumes
- Task is instantiation of task definition, running containers as single unit
- Service maintains desired number of tasks with auto-scaling, load balancing, and rolling updates
- EC2 launch type provides control over instances with responsibility for cluster management
- Fargate launch type eliminates server management with per-task pricing
- Service discovery enables tasks to find each other via DNS names
- ECS Anywhere extends ECS to customer-managed infrastructure
- Capacity providers manage scaling of infrastructure for EC2 launch type

### EKS
- EKS (Elastic Kubernetes Service) provides managed Kubernetes control plane across multiple AZs
- Control plane runs in AWS-managed account, automatically updated and scaled
- Worker nodes can be self-managed, managed node groups, or Fargate
- EKS manages Kubernetes API server, etcd, and scheduler with high availability
- aws-node (VPC CNI) plugin assigns VPC IP addresses to pods natively
- IAM roles for service accounts (IRSA) provide fine-grained permissions to Kubernetes pods
- EKS managed node groups automate provisioning and lifecycle of worker nodes
- EKS Anywhere runs Kubernetes on-premises with consistent AWS tooling
- EKS Distro provides Kubernetes distribution used by EKS for consistent deployments
- Fargate profiles specify which pods run on Fargate serverless infrastructure

### Fargate
- AWS Fargate is serverless compute engine for containers on ECS and EKS
- Eliminates node management, scaling, and capacity planning for container infrastructure
- Each task or pod runs in isolated runtime environment with dedicated resources
- Pricing based on vCPU and memory resources consumed, not underlying instances
- Task networking assigns each task its own elastic network interface with security group
- Fargate integrates with ECS services for load balancing, auto-scaling, and rolling updates
- For EKS, Fargate profiles specify namespaces to run as Fargate pods
- Fargate tasks support ephemeral storage with configurable size (20-200GB)
- Image pull caching improves startup time for frequently used container images
- Fargate Windows containers support Windows-based workloads in serverless environment

## Storage

### S3 architecture
- Amazon S3 (Simple Storage Service) provides object storage with 99.999999999% (11 9's) durability
- Objects stored in buckets, with globally unique bucket names across all AWS accounts
- Object key uniquely identifies object within bucket, with prefix for organization
- S3 API supports GET, PUT, DELETE, LIST operations with REST and SOAP interfaces
- Consistency model provides read-after-write consistency for PUTS of new objects
- Eventual consistency for overwrite PUTS and DELETES (updated to strong consistency in 2020)
- S3 Versioning preserves previous object versions, protecting against accidental deletion
- S3 Object Lock enforces WORM (write-once-read-many) retention for compliance
- S3 Inventory provides list of objects and metadata for auditing and compliance
- Multipart upload enables efficient upload of large objects with parallel parts

### S3 storage classes
- S3 Standard for frequently accessed data with low latency and high throughput
- S3 Intelligent-Tiering automatically moves data between access tiers based on usage patterns
- S3 Standard-IA (Infrequent Access) for data accessed less frequently but requiring rapid access
- S3 One Zone-IA for re-creatable infrequently accessed data (lower cost, no multi-AZ resilience)
- S3 Glacier Instant Retrieval for archive data requiring millisecond access
- S3 Glacier Flexible Retrieval for archive data with minutes to hours retrieval
- S3 Glacier Deep Archive for lowest-cost storage with 12-48 hours retrieval
- S3 Outposts stores objects on-premises for local data residency requirements
- S3 on Outposts provides S3 API compatibility in customer data centers
- Storage class analysis monitors access patterns to recommend optimal tier transitions

### S3 lifecycle policies
- Lifecycle policies automate transitioning objects between storage classes
- Transition actions move objects to different storage classes as they age
- Expiration actions delete objects after specified time periods
- Abort incomplete multipart uploads after specified time
- Version lifecycle manages both current and previous versions separately
- Minimum storage duration requirements prevent excessive transitions
- Tag-based filters apply policies only to objects with specific tags
- Size-based filters exclude small objects that may not be cost-effective to transition
- Date-based rules trigger actions on specific calendar dates
- Lifecycle analysis tools help simulate costs before implementing policies

### EBS
- Amazon EBS (Elastic Block Store) provides persistent block storage for EC2 instances
- Volume types include gp3/gp2 (general purpose), io2/io1 (provisioned IOPS), st1 (throughput optimized), sc1 (cold)
- Snapshots backup volumes to S3 incrementally for point-in-time recovery
- Encryption at rest uses KMS keys with integration with AWS KMS
- Multi-attach enables attaching single io1/io2 volume to multiple EC2 instances (limited to Nitro instances)
- EBS-optimized instances provide dedicated throughput for EBS I/O
- Provisioned IOPS guarantee consistent performance for I/O-intensive applications
- Snapshots are incremental, storing only changed blocks since last snapshot
- Fast snapshot restore eliminates latency on new volume creation from snapshot
- EBS direct API enables reading/writing snapshots directly without volumes

### EFS
- Amazon EFS (Elastic File System) provides scalable NFS file storage for EC2 instances
- File system grows and shrinks automatically as files added/removed
- Performance modes include General Purpose (latency-optimized) and Max I/O (highly parallel)
- Throughput modes include Bursting (throughput scales with storage) and Provisioned
- Storage classes include Standard and Infrequent Access (IA) with lifecycle management
- EFS Access Points simplify application access with defined directory paths and POSIX users
- EFS integrates with AWS Backup for centralized backup management
- Mount targets in each AZ provide highly available access from within VPC
- Encryption at rest and transit secures file system data
- EFS replication asynchronously replicates to another region for disaster recovery

### Glacier
- Amazon S3 Glacier is secure, durable, and low-cost storage for data archiving
- Vault is container for archives with configurable access policies
- Archives can be any object like photos, videos, files up to 40TB
- Vault Lock enforces compliance controls through WORM policies
- Retrieval options include Expedited (1-5 minutes), Standard (3-5 hours), and Bulk (5-12 hours)
- Provisioned capacity ensures expedited retrievals are available when needed
- Glacier Select runs queries directly on archived data without retrieval
- S3 Lifecycle policies automatically transition objects to Glacier
- Glacier Deep Archive offers lowest-cost storage with 12-hour retrieval
- Data is redundantly stored across multiple AZs for 99.999999999% durability

### Backup strategies
- AWS Backup provides centralized backup management across AWS services
- Backup plans define schedule, retention, and lifecycle rules
- Backup vaults store backups with encryption and access policies
- Cross-region backup copies backups to different region for disaster recovery
- Cross-account backup enables backup sharing between accounts
- Resource assignments define which resources are backed up by which plans
- On-demand backups create immediate point-in-time backups
- Backup policies (in Organizations) automate backup configuration across accounts
- Legal hold preserves backups indefinitely for compliance or litigation
- Restore testing automates validation of backup recoverability

## Networking

### VPC architecture
- Amazon VPC (Virtual Private Cloud) provides logically isolated network in AWS cloud
- VPC spans all Availability Zones in the region, with subnets in specific AZs
- IP addressing uses CIDR blocks (e.g., 10.0.0.0/16) for VPC and subnet ranges
- Route tables control traffic routing between subnets, internet, and VPN connections
- Internet Gateway (IGW) enables communication between VPC and internet
- Virtual Private Gateway (VGW) connects VPC to on-premises via VPN or Direct Connect
- VPC endpoints privately connect to AWS services without internet
- Network Address Translation (NAT) enables private subnet instances to access internet
- DHCP options sets provide DNS and domain name configuration to instances
- VPC Flow Logs capture IP traffic information for security and troubleshooting

### Subnets
- Subnets segment VPC IP address range within single Availability Zone
- Public subnets have route to Internet Gateway for direct internet access
- Private subnets have no direct internet route, accessing internet via NAT Gateway
- Subnet types include public, private, VPN-only, and isolated (no connectivity)
- Each subnet maps to single Availability Zone for fault isolation
- Auto-assign public IPv4 address enables instances in subnet to receive public IPs
- Subnet routing uses route tables that can be main (default) or custom
- Network ACLs apply stateless firewall rules at subnet boundary
- Subnet sizing requires planning for growth without renumbering
- Reserved IP addresses (first 4 and last 1 in each CIDR) cannot be assigned to instances

### Route tables
- Route tables contain rules (routes) determining where network traffic is directed
- Each subnet must be associated with exactly one route table
- Main route table is default for subnets not explicitly associated
- Local route automatically enables communication within VPC
- Routes include destination CIDR and target (IGW, NAT, VPC peering, etc.)
- Route propagation automatically adds VPN routes from Virtual Private Gateway
- Edge associations route traffic through AWS edge services
- Prefix lists group multiple CIDR blocks for simplified route management
- Route priority uses most specific route (longest prefix match)
- Gateway route tables control routing for gateways (Internet Gateway, Virtual Private Gateway)

### Internet gateways
- Internet Gateway (IGW) enables communication between VPC and internet
- Horizontally scaled, redundant, and highly available VPC component
- Performs network address translation for instances with public IPs
- One IGW can attach to only one VPC at a time
- IGW serves as target in route tables for internet-bound traffic
- Supports both IPv4 and IPv6 communication
- No additional bandwidth limitations or costs (only data transfer charges)
- Egress-Only Internet Gateway provides outbound-only IPv6 access
- Instances need public IP or Elastic IP for internet communication through IGW
- IGW can be detached from VPC, disconnecting all internet access

### NAT gateways
- NAT Gateway enables instances in private subnets to access internet
- Managed service by AWS, automatically highly available in single AZ
- Requires Elastic IP and resides in public subnet
- Processes traffic from private subnets, hiding private IPs behind NAT
- Supports TCP, UDP, and ICMP protocols (no port forwarding or load balancing)
- Bandwidth scales automatically up to 45 Gbps
- Cannot be used as bastion host or for inbound connections
- NAT Gateway in each AZ provides AZ-level redundancy
- Costs include hourly charge and data processing fees
- NAT instances (EC2-based) offer alternative with more control but require management

### Security groups
- Security groups act as virtual stateful firewalls for EC2 instances
- Default deny all inbound, allow all outbound
- Rules specify source (CIDR, security group, prefix list), protocol, and port
- Stateful nature means return traffic automatically allowed regardless of rules
- Multiple security groups can be attached to single instance (evaluated together)
- Security groups can reference each other for rule simplification
- Up to 5 security groups per network interface (default limit, can increase)
- Rules evaluated all together; no priority ordering
- Can reference security groups in other accounts (with proper configuration)
- Security group rules support IPv4 and IPv6 CIDR notation

### Network ACLs
- Network ACLs are stateless firewall rules at subnet level
- Both inbound and outbound rules evaluated separately
- Rules processed in order by rule number (lowest to highest)
- Each subnet must be associated with exactly one NACL (default NACL if none specified)
- Stateless nature requires explicit rules for return traffic
- Default NACL allows all inbound and outbound traffic
- Custom NACLs initially deny all traffic until rules added
- Supports allow and deny rules (security groups only allow)
- Ephemeral ports must be explicitly allowed for return traffic
- NACL logging through Flow Logs captures traffic at subnet boundary

### VPC peering
- VPC peering connects two VPCs for private communication using AWS network
- No single point of failure or bandwidth bottleneck
- Peering connections are not transitive (A to B to C doesn't enable A to C)
- VPCs can be in same or different accounts and regions
- CIDR blocks cannot overlap between peered VPCs
- Route tables must be updated for instances to communicate across peering
- Security groups can reference peered VPC security groups
- Cross-region VPC peering enables VPC communication across regions
- Maximum peering connections per VPC is 125 (configurable limit)
- DNS resolution across peering can be enabled for private hosted zones

### Transit Gateway
- Transit Gateway acts as hub for connecting multiple VPCs and on-premises networks
- Simplifies network architecture, replacing complex peering mesh
- Supports transitive routing between all connected networks
- Route tables control traffic between different attachments
- VPN attachments connect to on-premises via Site-to-Site VPN
- Direct Connect Gateway attachments integrate with Direct Connect
- VPC attachments connect VPCs using Transit Gateway
- Multicast support enables multicast traffic between VPCs
- Cross-account sharing via AWS Resource Access Manager
- Network Manager provides single dashboard for global network management

### Direct Connect
- AWS Direct Connect provides dedicated private connection from on-premises to AWS
- Bypasses internet, reducing network costs and increasing bandwidth consistency
- Connection speeds from 50 Mbps to 100 Gbps
- Virtual interfaces (VLANs) provision multiple connections over single circuit
- Private VIF accesses resources in VPC privately
- Public VIF accesses public AWS services (S3, DynamoDB) privately
- Transit VIF connects to Transit Gateway for multi-VPC access
- Direct Connect Gateway provides single connection to multiple VPCs across regions
- Link Aggregation Groups (LAG) combine multiple connections for higher availability
- Location requirements include colocation facility or partner-provided connectivity

### CloudFront
- Amazon CloudFront is global content delivery network (CDN) service
- Edge locations cache content close to users for low-latency delivery
- Origin can be S3, EC2, Elastic Load Balancer, or custom HTTP server
- Behaviors route different URL patterns to different origins
- Lambda@Edge runs code at edge locations for customization
- CloudFront Functions provide lightweight JavaScript for high-scale manipulations
- Field-level encryption encrypts sensitive data at edge before forwarding
- Geo-restriction blocks or allows content by country
- Signed URLs and cookies secure premium content
- Real-time logs provide detailed request information for analysis

## Databases

### RDS architecture
- Amazon RDS (Relational Database Service) simplifies database administration tasks
- Supports multiple engines: MySQL, PostgreSQL, MariaDB, Oracle, SQL Server
- Automated backups with point-in-time recovery within retention period
- Automated patching of database engine and OS
- Multi-AZ deployments provide high availability with automatic failover
- Read replicas offload read traffic and improve performance
- Provisioned storage with ability to scale without downtime
- Automated monitoring with CloudWatch metrics and enhanced monitoring
- Encryption at rest with KMS and in transit with SSL/TLS
- Maintenance windows for updates with minimal disruption

### Aurora
- Amazon Aurora is MySQL and PostgreSQL-compatible relational database built for cloud
- Separates compute and storage with distributed, fault-tolerant storage system
- Storage automatically grows up to 128 TB per database instance
- Six copies of data across three Availability Zones for durability
- Automatic failover typically under 30 seconds with no data loss
- Read replicas up to 15 with low replica lag
- Aurora Global Database spans multiple regions for disaster recovery
- Backtrack rewinds database to specific point in time without restoring
- Aurora Serverless automatically scales compute capacity based on demand
- Parallel Query pushes down analytics queries to storage layer

### DynamoDB
- Amazon DynamoDB is fully managed NoSQL key-value and document database
- Single-digit millisecond performance at any scale
- Tables scale horizontally with partition keys distributing data
- Items are collections of attributes with flexible schemas
- On-demand capacity automatically accommodates traffic without capacity planning
- Provisioned capacity with auto scaling for predictable workloads
- DAX (DynamoDB Accelerator) provides in-memory cache for microsecond latency
- Global tables provide multi-region, multi-primary replication
- Transactions support ACID across multiple items
- DynamoDB Streams capture table activity for event-driven architectures

### Redshift
- Amazon Redshift is fully managed petabyte-scale data warehouse
- Columnar storage optimizes analytical queries over large datasets
- Massively parallel processing (MPP) distributes queries across nodes
- Leader node coordinates queries, compute nodes execute and store data
- Distribution styles (KEY, EVEN, ALL) determine data placement across nodes
- Sort keys optimize query performance by organizing data on disk
- Redshift Spectrum queries data directly in S3 without loading
- Concurrency scaling automatically adds capacity for high query volumes
- RA3 nodes separate compute and storage with managed storage
- Automated backups with snapshots to S3 for disaster recovery

### ElastiCache
- Amazon ElastiCache provides managed Redis and Memcached in-memory caches
- Redis supports data structures, persistence, replication, and high availability
- Memcached offers simple, distributed cache with multi-threaded performance
- Cluster mode for Redis supports sharding across multiple nodes
- Read replicas scale read throughput and provide failover target
- Backup and restore for Redis enables point-in-time recovery
- Auto-discovery simplifies client configuration for Memcached clusters
- Security groups, encryption, and VPC isolation protect cache data
- ElastiCache for Redis Global Datastore provides cross-region replication
- Monitoring through CloudWatch metrics and slow log

### Database scaling strategies
- Read replicas distribute read traffic across multiple copies
- Vertical scaling increases instance size for more CPU/memory/IOPS
- Horizontal scaling (sharding) distributes data across multiple databases
- Connection pooling reduces overhead of establishing database connections
- Caching reduces database load by serving frequent queries from memory
- Database partitioning splits tables into smaller, manageable pieces
- Index optimization ensures queries use efficient access paths
- Denormalization reduces joins for read-heavy workloads
- Database federation routes queries to specialized database engines
- Archive and purge removes old data from operational databases

## Security

### IAM roles and policies
- IAM roles provide temporary credentials for trusted entities to assume
- Trust policy defines who can assume the role (users, services, federated identities)
- Permission policy defines what actions assumed role can perform
- Service roles allow AWS services to access resources on your behalf
- EC2 instance roles provide permissions to applications on EC2 without keys
- Cross-account roles enable access to resources in other accounts
- Session policies restrict permissions for specific role sessions
- PassRole permission controls which roles can be passed to services
- Role chaining occurs when roles assume other roles (limited to 1-hour session)
- Service-linked roles are predefined by AWS services with specific permissions

### KMS
- AWS KMS (Key Management Service) creates and manages encryption keys
- Customer master keys (CMKs) are logical representations of master keys
- Automatic key rotation (yearly) for AWS-managed and customer-managed CMKs
- Hardware Security Modules (HSMs) protect key material FIPS 140-2 Level 3
- Envelope encryption encrypts data with data keys protected by CMKs
- Key policies control access to CMKs, working with IAM
- Grants provide temporary permissions to use keys
- Custom key stores use CloudHSM behind your KMS keys
- Import your own key material for compliance requirements
- KMS integrates with over 100 AWS services for encryption

### Secrets Manager
- AWS Secrets Manager securely stores, rotates, and manages secrets
- Secrets include database credentials, API keys, and other sensitive configuration
- Automatic rotation integrates with AWS databases and custom Lambda functions
- Encryption uses KMS with optional customer-managed keys
- Fine-grained access control through IAM and resource policies
- Secret versioning tracks multiple versions with staging labels
- Cross-account access enables secret sharing between accounts
- Secrets Manager integrates with CloudFormation for infrastructure as code
- Rotation schedules (30-365 days) enforce credential lifecycle
- Secret value retrieval through SDK, CLI, or console with audit trail

### WAF
- AWS WAF (Web Application Firewall) protects web applications from common exploits
- Rules inspect HTTP/HTTPS requests to allow, block, or count
- SQL injection protection filters malicious database queries
- Cross-site scripting (XSS) protection prevents script injection
- Rate-based rules block excessive requests from single IP
- Geo-match rules block or allow countries
- IP reputation lists from AWS Managed Rules block known bad actors
- Rule groups combine related rules for easier management
- WAF integrates with CloudFront, ALB, API Gateway, and AppSync
- Bot Control managed rules identify and manage bot traffic

### Shield
- AWS Shield provides managed DDoS protection for AWS resources
- Shield Standard automatically protects all AWS customers at no cost
- Standard protection includes network/transport layer DDoS attacks
- Shield Advanced provides enhanced protection for specific resources
- Advanced includes cost protection against scaling charges from DDoS
- DDoS Response Team (DRT) access for incident support
- Advanced monitoring and attack visibility through CloudWatch
- Application layer protection for CloudFront and Route 53
- Proactive engagement from AWS during attacks for Advanced customers
- Health-based detection uses Route 53 health checks for application availability

### GuardDuty
- Amazon GuardDuty is intelligent threat detection service
- Analyzes CloudTrail logs, VPC Flow Logs, and DNS logs
- Machine learning models identify anomalous behavior
- Threat intelligence feeds include known malicious IPs and domains
- Findings detail suspicious activities with severity levels
- Automated response through EventBridge triggering remediation
- Malware protection scans EC2 volumes for malicious files
- Runtime monitoring detects threats within container workloads
- Organizations integration aggregates findings across accounts
- Suppression rules filter out non-actionable findings

### Security best practices
- Principle of least privilege grants minimum permissions necessary
- Enable multi-factor authentication for all users, especially root
- Use IAM roles for applications instead of long-term access keys
- Enable encryption at rest and in transit for all data
- Implement network segmentation with VPCs and security groups
- Enable CloudTrail in all regions for audit trail
- Use AWS Config for continuous compliance monitoring
- Rotate credentials regularly with automated processes
- Implement data classification and protection policies
- Regularly review and audit IAM permissions and unused resources

## Monitoring & DevOps

### CloudWatch
- Amazon CloudWatch monitors AWS resources and applications
- Metrics collect data points from resources (CPU utilization, disk I/O)
- Custom metrics from applications extend monitoring beyond infrastructure
- Alarms trigger actions when metrics cross thresholds
- Dashboards create customizable views of multiple metrics
- Logs ingest, store, and monitor log files from various sources
- Log Insights queries and analyzes log data interactively
- Events (now EventBridge) triggers actions based on state changes
- Contributor Insights analyzes log data to identify top contributors
- Synthetics creates canaries to monitor endpoints and APIs

### CloudTrail
- AWS CloudTrail records API activity across AWS infrastructure
- Event history contains last 90 days of management events
- Trails deliver events to S3 for long-term retention
- Management events track operations on AWS resources
- Data events track resource operations (S3 object-level, Lambda function)
- Insights events identify unusual API activity
- Organization trails log all accounts in AWS Organization
- Log file integrity validation ensures logs haven't been modified
- Event selectors control which events are logged
- CloudTrail Lake provides managed data lake for audit and security

### AWS Config
- AWS Config assesses, audits, and evaluates resource configurations
- Configuration items record resource state at points in time
- Configuration history tracks changes over time
- Configuration snapshots capture state of all resources
- Config rules evaluate resources against desired configurations
- Managed rules provided by AWS for common compliance checks
- Custom rules use Lambda for specific compliance requirements
- Conformance packs aggregate rules across accounts and regions
- Remediation actions automatically fix non-compliant resources
- Aggregators view compliance across multiple accounts and regions

### Infrastructure as code
- Infrastructure as Code (IaC) manages infrastructure through machine-readable definition files
- AWS CloudFormation provides declarative IaC for AWS resources
- AWS CDK (Cloud Development Kit) uses programming languages to define infrastructure
- HashiCorp Terraform supports multi-cloud infrastructure as code
- AWS SAM (Serverless Application Model) extends CloudFormation for serverless
- CloudFormation StackSets deploy across multiple accounts and regions
- Change sets preview infrastructure changes before execution
- Drift detection identifies manual changes to CloudFormation-managed resources
- Nested stacks compose complex architectures from reusable templates
- Custom resources extend CloudFormation to non-AWS or custom provisioning

### CloudFormation
- AWS CloudFormation provisions AWS resources using templates
- Templates (JSON/YAML) describe resources and their configuration
- Stacks manage collection of resources as single unit
- Stack updates modify resources with controlled change sets
- Change sets preview effects of stack updates before execution
- Stack policies prevent updates to critical resources
- Stack sets deploy stacks across multiple accounts and regions
- Resource imports bring existing resources under CloudFormation management
- Mappings enable conditional values based on region, environment, etc.
- Outputs export stack values for use in other stacks

### CDK
- AWS CDK (Cloud Development Kit) defines cloud infrastructure in programming languages
- Supported languages include TypeScript, Python, Java, C#, Go
- Constructs are reusable cloud components with different abstraction levels
- L1 constructs correspond directly to CloudFormation resources
- L2 constructs provide sensible defaults and convenience methods
- L3 constructs (patterns) define complete architectural patterns
- CDK CLI synthesizes code into CloudFormation templates
- CDK apps can include unit tests for infrastructure code
- Context values provide environment-specific configuration
- CDK Pipelines automates deployment using CDK-defined CI/CD

### CI/CD pipelines
- AWS CodeCommit provides managed Git repositories
- AWS CodeBuild builds and tests code in fully managed environment
- AWS CodeDeploy automates deployments to EC2, Lambda, or on-premises
- AWS CodePipeline orchestrates end-to-end release process
- Pipeline stages include source, build, test, and deploy
- Manual approvals gate deployments for control
- Cross-account pipelines deploy to multiple environments
- Integration with third-party tools (GitHub, Jenkins, Jenkins)
- Pipeline actions run in sequence or parallel
- CloudWatch Events trigger pipelines on source changes

## Advanced AWS

### Multi-account architecture
- Multi-account strategy isolates workloads, environments, and teams
- AWS Organizations centrally govern multiple accounts
- Organizational units (OUs) group accounts for policy application
- Service control policies (SCPs) restrict maximum permissions
- Account baselines automate new account provisioning
- Log archive account stores CloudTrail and Config logs centrally
- Security tooling account hosts GuardDuty, Security Hub, etc.
- Network account manages shared networking resources
- Workload accounts host applications with appropriate isolation
- AWS Control Tower automates landing zone setup and governance

### Serverless design patterns
- Event-driven microservices use events to communicate between services
- API Gateway + Lambda create serverless REST or HTTP APIs
- Step Functions orchestrate workflows with error handling and retries
- Fan-out patterns use SNS or EventBridge for parallel processing
- Queue-based load leveling with SQS smooths traffic spikes
- Saga pattern for distributed transactions across services
- CQRS with DynamoDB for separate read/write models
- Stream processing with Kinesis and Lambda for real-time analytics
- Throttling and rate limiting with API Gateway usage plans
- Idempotency keys prevent duplicate processing

### EventBridge
- Amazon EventBridge is serverless event bus for application integration
- Events represent changes in environment as JSON objects
- Event buses receive events (default, custom, partner)
- Rules match events and route to targets
- Targets include Lambda, Step Functions, SQS, SNS, and over 20 others
- Partner event sources integrate with SaaS applications (Zendesk, Datadog)
- Schema registry discovers and stores event structures
- Schema discovery generates code bindings for events
- Archive and replay events for debugging and reprocessing
- EventBridge Pipes provides point-to-point integration with transformation

### Step Functions
- AWS Step Functions coordinates multiple AWS services into workflows
- Workflows are state machines defined in Amazon States Language
- Standard workflows run for up to one year with exactly-once execution
- Express workflows run for up to five minutes with at-least-once execution
- Task states perform single unit of work (Lambda, Batch, ECS, etc.)
- Choice states add branching logic to workflows
- Parallel states execute branches concurrently
- Wait states pause execution for specified time
- Error handling with retry and catch for resilience
- Callback patterns integrate human approval or third-party processes

### API Gateway
- Amazon API Gateway creates, publishes, and manages APIs
- REST APIs follow RESTful principles with standard HTTP methods
- HTTP APIs provide lower-latency, lower-cost API option
- WebSocket APIs maintain persistent connections for real-time communication
- API endpoints include edge-optimized, regional, and private
- Usage plans throttle and meter API access by API key
- Request validation checks payloads before backend invocation
- Response caching reduces backend load and latency
- API versions managed through stage variables and canary deployments
- OpenAPI import and export for API definition management

### Data lakes on AWS
- Data lakes store structured and unstructured data at scale
- Amazon S3 serves as central data lake storage layer
- AWS Glue discovers, catalogs, and transforms data
- Glue Data Catalog provides metadata repository for data assets
- Lake Formation manages data lake access and permissions
- Amazon Athena queries data directly in S3 using SQL
- Amazon EMR processes large datasets with frameworks like Spark
- Amazon Redshift Spectrum queries data lake from data warehouse
- Amazon QuickSight visualizes data lake data
- AWS Lake Formation Blueprints automate data ingestion workflows

### High availability design
- Multi-AZ deployment distributes resources across Availability Zones
- Multi-region architecture protects against region failures
- Load balancing distributes traffic across healthy instances
- Auto-scaling maintains capacity during instance failures
- Health checks detect failures and trigger recovery
- Stateless applications simplify failover and scaling
- Database replication ensures data availability during failures
- Data backup with cross-region copies for disaster recovery
- Chaos engineering tests resilience through controlled experiments
- SLA calculation ensures composite availability meets requirements

### Disaster recovery on AWS
- Backup and restore copies data to S3 or other regions
- Pilot light maintains minimal core infrastructure ready for scale-up
- Warm standby runs scaled-down production environment continuously
- Multi-site active-active runs production in multiple regions
- Recovery Time Objective (RTO) drives architecture decisions
- Recovery Point Objective (RPO) determines data replication requirements
- AWS Elastic Disaster Recovery (CloudEndure) automates server replication
- Route 53 health checks and routing policies enable traffic failover
- Database replication (Aurora Global, DynamoDB Global Tables)
- Disaster recovery testing through regular drills and automation

### Cost optimization
- Rightsizing matches instance types to workload requirements
- Auto-scaling eliminates idle capacity during low demand
- Reserved Instances and Savings Plans reduce costs for steady-state workloads
- Spot Instances run fault-tolerant workloads at significant discounts
- S3 lifecycle policies transition data to cheaper storage tiers
- Data transfer optimization reduces cross-region and internet traffic
- Resource tagging enables cost allocation and chargeback
- AWS Cost Explorer analyzes spending patterns and trends
- AWS Budgets alert on cost thresholds and anomalies
- Trusted Advisor identifies optimization opportunities

### Performance tuning
- Load testing identifies bottlenecks under expected and peak loads
- Profiling tools (AWS X-Ray, CodeGuru) identify slow code paths
- Database optimization through indexing, query tuning, and caching
- Compute optimization selects appropriate instance families and sizes
- Network optimization uses enhanced networking and placement groups
- Content delivery through CloudFront reduces origin load
- Connection pooling reduces database connection overhead
- Asynchronous processing offloads slow operations from request path
- Monitoring establishes baselines and measures improvements
- Auto-scaling adjusts capacity to maintain performance during spikes

# Azure

## Core Azure Concepts

### Azure global infrastructure
- Azure operates 60+ regions worldwide, more than any other cloud provider
- Region pairs (e.g., East US/West US) are at least 300 miles apart for disaster recovery
- Availability Zones are physically separate locations within region with independent power and cooling
- Azure geographies divide world into markets with data residency and compliance boundaries
- ExpressRoute locations connect on-premises to Azure through private connections
- Azure Edge Zones provide CDN and compute at the edge for low-latency applications
- Azure Stack extends Azure services to on-premises and edge environments
- Sovereign clouds include Azure Government, Azure China (21Vianet), and Azure Germany
- Azure regions are connected through Microsoft global network with multiple fiber paths
- Data sovereignty ensures customer data stays within selected geography

### Azure resource hierarchy
- Management groups provide hierarchical organization of subscriptions for governance
- Subscriptions group resources and serve as billing and scale boundaries
- Resource groups contain related resources for a solution with shared lifecycle
- Resources are individual service instances (VMs, databases, functions)
- Resource providers offer specific Azure services (Microsoft.Compute, Microsoft.Storage)
- Resource types define configuration schema for each Azure service
- Resource Manager (ARM) is deployment and management layer for all resources
- Resource locks prevent accidental deletion or modification of critical resources
- Tags organize resources for billing, management, and automation
- Resource Graph enables querying resources across subscriptions efficiently

### Subscriptions and resource groups
- Subscriptions provide authenticated and authorized access to Azure services
- Account administrator manages billing for subscription
- Service administrator has full access to resources in subscription
- Co-administrators can be added for shared management
- Subscriptions can have different trust boundaries (dev/test, production)
- Resource groups collect resources with same lifecycle and management
- Resources can only exist in one resource group
- ARM templates deploy and update all resources in resource group
- Resource group location stores metadata about resources
- Moving resources between groups requires careful planning for dependencies

### Azure pricing model
- Pay-as-you-go charges for compute by second with no upfront commitment
- Reserved Instances provide discounts up to 72% for one or three-year terms
- Azure Hybrid Benefit uses existing Windows Server/SQL Server licenses
- Spot Virtual Machines offer unused capacity at deep discounts
- Azure Cost Management analyzes and optimizes cloud spending
- Pricing calculator estimates costs for planned deployments
- Total Cost of Ownership (TCO) calculator compares Azure to on-premises
- Azure Budgets set spending limits with alerts
- Azure Advisor provides cost optimization recommendations
- Dev/test subscription pricing provides discounted rates for non-production workloads

### Azure portal
- Azure portal is web-based unified console for managing Azure resources
- Dashboard provides customizable view of resources and metrics
- Resource browser navigates through all resources in subscriptions
- Cloud Shell provides browser-based CLI and PowerShell access
- Azure Quickstart Center guides new users through setup
- Service health displays status of Azure services globally
- Azure Advisor provides personalized best practice recommendations
- Microsoft Entra ID (formerly Azure AD) manages identities
- Azure Monitor integrates monitoring across all resources
- Azure Marketplace deploys third-party solutions and services

### Azure CLI
- Azure CLI is cross-platform command-line tool for Azure management
- Available for Windows, macOS, and Linux with installers
- Interactive mode provides command completion and descriptions
- Output formats include JSON, table, and TSV for scripting
- Azure CLI can run in Cloud Shell without local installation
- Query with JMESPath filters output for specific values
- Role-based access control authentication with Microsoft Entra ID
- Extensions add preview and experimental features
- Azure CLI supports multiple subscriptions with context switching
- Automation scripts use Azure CLI for repeatable deployments

### Azure governance
- Azure Policy enforces rules and effects over resources
- Policy definitions include allowed locations, SKU sizes, and tagging requirements
- Initiatives group related policies for comprehensive compliance
- Policy assignment applies policies to specific scopes
- Compliance view shows policy adherence across resources
- Remediation tasks fix non-compliant resources automatically
- Azure Blueprints orchestrates deployment of policy, RBAC, and ARM templates
- Management groups organize subscriptions for policy inheritance
- Azure Role-Based Access Control (RBAC) governs access permissions
- Azure Governance Visualizer (AZ Gov) assesses governance posture

## Compute

### Virtual machines
- Azure Virtual Machines provide IaaS with Windows or Linux operating systems
- VM series optimized for different workloads: general purpose, compute, memory, storage, GPU
- Availability Sets distribute VMs across fault and update domains
- Availability Zones protect against data center failures
- Scale Sets automatically manage identical VMs for scaling
- VM extensions configure software, security, and monitoring after deployment
- Azure Bastion provides secure RDP/SSH access without public IPs
- Boot diagnostics capture serial console output for troubleshooting
- Azure Dedicated Host provides physical servers for compliance
- Azure Spot VMs offer discounted interruptible instances

### VM scale sets
- Virtual Machine Scale Sets manage identical, auto-scaling VMs
- Automatic scaling based on metrics (CPU, memory) or schedule
- Uniform orchestration manages identical instances with VMSS configuration
- Flexible orchestration supports mixed instance types and configurations
- Instance protection prevents specific VMs from scale-in events
- Application health extension monitors application responsiveness
- Automatic OS updates patch operating systems
- Custom script extension configures applications at scale
- Scale sets integrate with load balancers for traffic distribution
- Availability zones distribute scale set instances across zones

### Azure Functions
- Azure Functions is serverless compute for event-driven applications
- Consumption plan scales automatically and charges per execution
- Premium plan provides pre-warmed instances and VPC connectivity
- Dedicated plan runs functions on App Service plan
- Triggers invoke functions from events (HTTP, timer, blob, queue)
- Bindings connect functions to data sources declaratively
- Durable Functions orchestrate stateful workflows
- Functions runtime supports .NET, Java, Node.js, Python, PowerShell
- Managed dependencies automatically restores packages
- Application Insights monitors function performance and failures

### App Services
- Azure App Service is PaaS for web applications and APIs
- Supports .NET, Java, Node.js, Python, PHP, and Ruby
- Built-in auto-scaling handles varying traffic loads
- Deployment slots enable zero-downtime updates
- App Service Environment provides isolated, dedicated environment
- WebJobs run background tasks in App Service context
- Authentication/authorization integrates with Microsoft Entra ID and social providers
- Custom domains and TLS/SSL certificates managed through service
- VNet integration connects apps to private resources
- Application settings and connection strings configure apps without code changes

### AKS (Kubernetes)
- Azure Kubernetes Service (AKS) manages Kubernetes control plane
- Automated upgrades for Kubernetes versions and node images
- Cluster autoscaler adjusts node count based on workload demands
- Horizontal pod autoscaler scales pods based on metrics
- Azure Active Directory integration for Kubernetes authentication
- Managed identity eliminates credential management
- Azure Policy for AKS enforces governance on clusters
- Container insights monitors cluster and workload health
- Network policies control pod-to-pod communication
- Azure CNI or kubenet for pod networking options

### Container Instances
- Azure Container Instances (ACI) runs containers without managing servers
- Fast startup times in seconds for containerized applications
- Hyper-V isolation ensures multi-tenant security
- Linux and Windows containers supported
- Public IP address assignment for direct container access
- Persistent storage with Azure Files volumes
- Container groups coordinate multiple containers on same host
- Environment variables configure containers at runtime
- Resource limits define CPU and memory allocation
- Integration with Azure Virtual Networks for private connectivity

## Storage

### Blob storage
- Azure Blob Storage is object storage for unstructured data
- Containers organize blobs similar to directories
- Block blobs for streaming and storing documents
- Page blobs for random access files (VHDs)
- Append blobs for logging data
- Hot tier for frequently accessed data
- Cool tier for infrequently accessed data with 30-day minimum
- Cold tier for rarely accessed data with 90-day minimum
- Archive tier for backup with rehydration time
- Blob lifecycle management automates tier transitions

### File storage
- Azure Files provides fully managed file shares in cloud
- SMB and NFS protocols supported for compatibility
- Drive mapping from Windows, Linux, and macOS
- Azure File Sync caches files on-premises with cloud tiering
- Active Directory integration for identity-based access
- Snapshots provide point-in-time restore capability
- Soft delete protects against accidental deletion
- File shares up to 100 TiB per share
- Private endpoints secure access within VNet
- Azure Backup integration for file share backup

### Queue storage
- Azure Queue Storage provides message queuing for asynchronous processing
- Messages up to 64 KB stored in queues
- Simple API with HTTP/HTTPS access
- Visibility timeout prevents duplicate processing
- Peek messages without dequeuing for inspection
- Update message content while in queue
- Queue metadata for management information
- Shared Access Signatures (SAS) for delegated access
- Cloud queues designed for large-scale distributed applications
- At-least-once delivery guarantee with poison queue handling

### Disk storage
- Azure Managed Disks provide block-level storage for VMs
- Ultra disks provide highest performance with sub-millisecond latency
- Premium SSD for consistent high performance
- Standard SSD for moderate performance workloads
- Standard HDD for cost-effective bulk storage
- Disk encryption with server-side or Azure Disk Encryption
- Incremental snapshots save only changed blocks
- Shared disks attach to multiple VMs for clustered applications
- Disk bursting handles temporary performance spikes
- Azure Backup integration for disk backup

### Storage tiers
- Hot tier optimized for frequently accessed data
- Cool tier for infrequently accessed data stored at least 30 days
- Cold tier for rarely accessed data stored at least 90 days
- Archive tier for backup data stored at least 180 days
- Tier rehydration moves archived data to online tier
- Lifecycle policies automate tier transitions based on age
- Priority for archive rehydration (standard vs high)
- Account-level tier sets default for new blobs
- Blob-level tier overrides account default
- Tier pricing balances access costs against storage costs

### Backup and recovery
- Azure Backup protects VMs, files, and workloads
- Recovery Services vault stores backup data securely
- Backup policies define schedule and retention
- Application-consistent backups for VSS-aware applications
- File-level restore from VM backups
- Cross-region restore for disaster recovery
- Soft delete retains deleted backups for 14 days
- Azure Site Recovery orchestrates disaster recovery failover
- Replication to secondary region for DR protection
- Test failover validates DR without impacting production

## Networking

### Virtual networks
- Azure Virtual Networks (VNet) provide isolated network environments
- Address space defined using CIDR notation
- Subnets segment VNet for security and organization
- Network Security Groups (NSG) filter traffic at subnet/NIC
- VNet peering connects VNets within same region
- Global VNet peering connects VNets across regions
- Service endpoints secure access to Azure services
- Private Link exposes services privately in VNet
- VNet service tags identify Azure services for NSG rules
- DDoS Protection Basic (free) or Standard (enhanced) protects VNet

### Subnets
- Subnets divide VNet IP address space into segments
- Each subnet must have unique CIDR block within VNet
- Reserved IP addresses (first four and last one) in each subnet
- Subnet delegation assigns subnet to specific Azure services
- Service endpoints connect to Azure services from subnet
- Network Security Groups attach at subnet level
- Route tables associate with subnet for custom routing
- Subnet sizes planned for growth without renumbering
- PrivateLink support requires specific subnet configuration
- VNet service endpoints are configured per subnet

### NSGs
- Network Security Groups filter network traffic to Azure resources
- Security rules based on source/destination, port, and protocol
- Inbound and outbound rules processed separately
- Rules evaluated in priority order (lowest to highest)
- Default rules allow VNet and Azure Load Balancer traffic
- Service tags identify Azure services in rules
- Application Security Groups group VMs for rule application
- NSG flow logs capture traffic for analysis
- NSG attached to subnet or network interface
- Effective security rules show applied rules combination

### Load balancer
- Azure Load Balancer distributes traffic to VM instances
- Layer 4 load balancing (TCP/UDP) only
- Public load balancer handles internet traffic
- Internal load balancer handles private VNet traffic
- Load balancing rules distribute traffic to backend pool
- Health probes monitor backend instance availability
- Outbound rules configure SNAT for internet access
- HA ports load balance on all ports
- Standard SKU provides availability zones and larger scale
- Gateway Load Balancer integrates third-party appliances

### Application gateway
- Azure Application Gateway is Layer 7 load balancer
- HTTP/HTTPS traffic with web application firewall (WAF)
- URL-based routing directs traffic to different backends
- Cookie-based session affinity maintains user sessions
- SSL termination offloads encryption processing
- End-to-end SSL for secure communication
- Autoscaling adjusts capacity based on traffic
- Redirection (HTTP to HTTPS) and rewrite rules
- Multi-site hosting routes based on hostname
- WebSocket support for real-time applications

### VPN gateway
- Azure VPN Gateway connects on-premises to Azure over internet
- Site-to-Site VPN connects branch offices via IPsec/IKE
- Point-to-Site VPN connects individual clients
- VNet-to-VNet connects Azure VNets
- Active-Active configuration for high availability
- ExpressRoute coexistence with VPN for backup
- BGP dynamic routing for complex networks
- Custom IPsec/IKE policies for compliance
- VPN Gateway SKUs determine throughput and features
- Gateway Subnet hosts VPN gateway resources

### ExpressRoute
- Azure ExpressRoute provides private connection to Azure
- Bypasses internet for consistent performance
- Layer 2 or Layer 3 connectivity options
- ExpressRoute Direct connects at 10/100 Gbps
- ExpressRoute Global Reach connects on-premises globally
- ExpressRoute FastPath improves data path performance
- Microsoft peering accesses public services
- Private peering accesses VNets privately
- ExpressRoute Gateway connects VNets to circuit
- Service provider partners facilitate last-mile connectivity

### Azure DNS
- Azure DNS hosts domains and provides name resolution
- Domain registration through Azure or third-party
- DNS zones host DNS records for domain
- Record sets include A, AAAA, CNAME, MX, TXT
- Alias records integrate with Azure resources
- Private DNS zones resolve names in VNets
- Autorenewal for registered domains
- DNSSEC for domain name security (third-party)
- Traffic Manager provides DNS-based load balancing
- Azure Front Door integrates global load balancing

## Databases

### Azure SQL
- Azure SQL Database is fully managed PaaS database
- SQL Server engine compatibility with latest features
- DTU (Database Transaction Unit) or vCore purchasing models
- Hyperscale tier for databases up to 100 TB
- Serverless compute auto-pauses during inactivity
- Geo-replication for disaster recovery
- Auto-failover groups enable application transparency
- Advanced Threat Protection detects anomalies
- Always Encrypted protects sensitive data
- Azure SQL Managed Instance provides near-100% SQL Server compatibility

### Cosmos DB
- Azure Cosmos DB is globally distributed, multi-model database
- Single-digit millisecond latency at 99th percentile
- Multi-region writes for active-active configurations
- Multiple APIs: SQL, MongoDB, Cassandra, Gremlin, Table
- Automatic and transparent partitioning
- Five consistency levels from strong to eventual
- Request Units (RU) provision throughput
- Serverless capacity for intermittent workloads
- Analytical store for HTAP scenarios
- Change feed for event-driven architectures

### PostgreSQL/MySQL services
- Azure Database for PostgreSQL - Flexible Server
- Azure Database for MySQL - Flexible Server
- Fully managed community editions with automated patching
- High availability with zone-redundant failover
- Read replicas for read scale-out
- Automated backups with point-in-time restore
- Connection pooling with PgBouncer (PostgreSQL)
- Private link for VNet connectivity
- Burstable SKUs for development
- Azure Cosmos DB for PostgreSQL (formerly Hyperscale)

### Synapse analytics
- Azure Synapse Analytics is integrated analytics service
- Dedicated SQL pool for data warehousing
- Serverless SQL pool for on-demand querying
- Apache Spark pools for data engineering
- Synapse Pipelines for data integration
- Data Explorer for log and time-series analytics
- Synapse Link for operational analytics
- Synapse Studio unified workspace
- Columnstore indexes for analytics performance
- Data distribution across compute nodes

### Database scaling
- Vertical scaling increases resources to existing database
- Horizontal scaling distributes across multiple databases
- Read replicas offload reporting and analytics
- Sharding partitions data across databases
- Connection pooling reduces connection overhead
- Hyperscale (Azure SQL) separates compute from storage
- Cosmos DB partitions automatically based on throughput
- Elastic pools share resources among multiple databases
- Geo-replication distributes globally
- Caching with Azure Redis Cache reduces database load

## Security

### Azure Active Directory
- Microsoft Entra ID (formerly Azure AD) is identity service
- Single sign-on for cloud and on-premises apps
- Multi-factor authentication enhances security
- Conditional Access evaluates risk for access decisions
- Identity Protection detects compromised identities
- Privileged Identity Management (PIM) for just-in-time access
- Self-service password reset reduces help desk load
- Application proxy publishes on-prem apps securely
- B2B collaboration invites external users
- B2C for customer identity management

### RBAC
- Azure Role-Based Access Control (RBAC) manages access
- Roles define permissions sets for operations
- Built-in roles (Owner, Contributor, Reader) for common needs
- Custom roles created for specific requirements
- Scope defines where role assignment applies
- Management group, subscription, resource group, resource scopes
- Deny assignments block specific actions
- Classic administrators for legacy access
- RBAC applies to control plane (management) operations
- Microsoft Entra roles for identity management separate from RBAC

### Key Vault
- Azure Key Vault securely stores secrets, keys, and certificates
- Hardware Security Module (HSM) options for FIPS 140-2 Level 2/3
- Key rotation policies automate credential updates
- Soft delete recovers accidentally deleted items
- Purge protection prevents permanent deletion
- Access policies control user and application access
- RBAC integration for Azure-wide permissions
- Managed HSM for single-tenant key management
- Key Vault references in ARM templates
- Logging and monitoring with Azure Monitor

### Defender for Cloud
- Microsoft Defender for Cloud is cloud security posture management (CSPM)
- Secure score assesses overall security posture
- Regulatory compliance benchmarks against standards
- Just-in-time VM access reduces attack surface
- Adaptive application controls allow-list applications
- File integrity monitoring tracks configuration changes
- Defender plans add workload-specific protection
- Security alerts detect threats in resources
- Vulnerability assessment scans for weaknesses
- Integration with Microsoft Sentinel for SIEM

### Security Center
- Azure Security Center (now integrated in Defender for Cloud)
- Unified security management across hybrid workloads
- Continuous assessment of security configurations
- Security recommendations with remediation steps
- Network map visualizes security groups
- Threat protection for PaaS services
- Fileless attack detection for VMs
- Adaptive network hardening recommends NSG rules
- Security policy management across subscriptions
- Integration with Azure Policy for governance

### Identity protection
- Microsoft Entra Identity Protection detects identity risks
- Risk detection includes leaked credentials, anonymous IP
- User risk policy blocks or requires password change
- Sign-in risk policy challenges with MFA
- Risk investigation with detailed reports
- Machine learning models improve detection over time
- Integration with Conditional Access for remediation
- Weekly digest emails summarize risk findings
- Risk-based remediation automates responses
- Identity Protection requires P2 licensing

## Monitoring & DevOps

### Azure Monitor
- Azure Monitor collects and analyzes telemetry
- Metrics from Azure resources (CPU, memory, disk)
- Logs from applications and operating systems
- Activity log tracks control plane operations
- Application Performance Monitoring (APM) with Application Insights
- Container insights monitor AKS and ACI
- VM insights monitor virtual machines
- Action groups notify and automate responses
- Workbooks create interactive reports
- Data collection rules configure log ingestion

### Log Analytics
- Log Analytics workspaces store monitoring data
- Kusto Query Language (KQL) queries analyze logs
- Tables organize different log types
- Log search across multiple workspaces
- Views visualize log data for dashboards
- Alert rules based on log search results
- Data export to Azure Storage or Event Hubs
- Pricing based on data ingestion and retention
- Workspace insights monitor workspace health
- Cross-workspace queries for comprehensive analysis

### Application Insights
- Application Insights monitors live applications
- Request rates, response times, and failure rates
- Dependency tracking for external services
- Distributed tracing across service boundaries
- Application maps visualize component interactions
- Profiler traces slow code paths
- Snapshot Debugger captures exception details
- Availability tests monitor endpoints globally
- Usage analytics tracks user behavior
- Integration with Visual Studio and Azure DevOps

### ARM templates
- ARM templates deploy Azure resources declaratively
- JSON format defines resources and configuration
- Parameters customize deployments per environment
- Variables simplify complex expressions
- Functions transform values at deployment
- Resources section defines deployed assets
- Outputs return values after deployment
- Template specs share templates within organization
- Deployment scripts run custom steps
- What-if operation previews changes before deployment

### Bicep
- Bicep is domain-specific language for ARM templates
- Simplified syntax compared to JSON
- Modular design with modules for reuse
- Type safety catches errors at compile time
- Automatic dependency management
- Linter enforces best practices
- Transpiles to standard ARM JSON
- Visual Studio Code extension provides intellisense
- Parameter files for environment configuration
- Deployment stacks manage resources as group

### DevOps pipelines
- Azure DevOps Services provides CI/CD tools
- Azure Pipelines builds and tests code automatically
- Multi-platform support (Windows, Linux, macOS)
- YAML or classic editor for pipeline definition
- Integration with GitHub, Bitbucket, and Azure Repos
- Release pipelines deploy to multiple environments
- Approvals and gates control deployments
- Continuous testing integrated in pipelines
- Azure Boards tracks work items
- Azure Artifacts hosts packages

## Advanced Azure

### Hybrid cloud architecture
- Azure Arc extends Azure management to on-premises
- Arc-enabled servers manage physical and virtual servers
- Arc-enabled Kubernetes manage Kubernetes clusters
- Arc-enabled data services run databases on-premises
- Azure Stack Hub provides Azure services on-premises
- Azure Stack HCI runs virtualized workloads on-premises
- Azure Stack Edge brings compute to edge devices
- VPN Gateway connects on-premises to Azure
- ExpressRoute provides private dedicated connection
- Azure File Sync caches files on-premises

### Azure Arc
- Azure Arc centralizes management of hybrid resources
- Azure Arc-enabled servers use Connected Machine agent
- Azure Policy applies governance to on-premises servers
- Azure Monitor collects logs and metrics from anywhere
- Arc-enabled Kubernetes supports GitOps configuration
- Arc-enabled data services run Azure SQL on-premises
- Azure Defender extends threat protection to on-premises
- Inventory and organization with tags and resource groups
- Role-based access control for on-premises resources
- Billing and licensing consolidated in Azure

### Multi-region architecture
- Traffic Manager provides DNS-based global load balancing
- Front Door offers application acceleration and WAF
- Cosmos DB multi-region writes for global databases
- SQL Database geo-replication for disaster recovery
- Azure Front Door routes traffic to closest region
- Storage geo-redundancy replicates data across regions
- Azure CDN caches content at edge locations
- Cross-region load balancing with Traffic Manager
- Active-active or active-passive regional configurations
- Region failover planning with Azure Site Recovery

### Disaster recovery
- Azure Site Recovery orchestrates replication and failover
- Replicate Azure VMs to secondary region
- Replicate on-premises VMs/physical servers to Azure
- Recovery plans sequence failover steps
- Test failover validates DR without disruption
- Failback returns workloads to primary site
- Azure Backup protects data independently
- Recovery Services vault holds backups and replicas
- Azure-to-Azure DR for regional failures
- Application-consistent recovery points

### High availability
- Availability Zones protect against data center failures
- Availability Sets distribute VMs across fault domains
- Load balancers distribute traffic across healthy instances
- Azure Front Door provides global load balancing
- Traffic Manager for DNS-based failover
- Auto-scaling maintains capacity during failures
- Azure SQL auto-failover groups for databases
- Storage redundancy (LRS, ZRS, GRS) options
- Health probes detect and route around failures
- Application design for graceful degradation

### Cost optimization
- Azure Advisor identifies cost optimization opportunities
- Reserved Instances reduce costs for steady workloads
- Azure Hybrid Benefit uses existing licenses
- Spot VMs for interruptible workloads
- Auto-shutdown stops idle VMs automatically
- Rightsizing recommendations match resources to usage
- Azure Cost Management analyzes spending patterns
- Budgets alert on cost thresholds
- Anomaly detection identifies unexpected spending
- Azure Pricing Calculator estimates costs

### Performance tuning
- Azure Load Testing simulates high-traffic scenarios
- Application Insights identifies performance bottlenecks
- Azure Monitor establishes performance baselines
- Azure Cache for Redis reduces database load
- CDN caches content closer to users
- Azure Front Door optimizes application delivery
- Azure SQL performance recommendations
- Autoscaling adjusts capacity to demand
- Azure Premium SSD provides consistent performance
- Network performance monitoring with Azure Monitor

# GCP

## Core GCP Concepts

### GCP global infrastructure
- Google Cloud operates regions across North America, South America, Europe, Asia, and Australia
- Regions consist of zones (minimum 3 per region) for fault isolation
- Zones within region connected with low-latency fiber optic networking
- Edge caching locations serve content through Google's global network
- Google's network carries approximately 40% of world's internet traffic
- Dedicated fiber backbone connects regions with high bandwidth
- Submarine cables provide intercontinental connectivity
- Google's network uses BGP Anycast for global load balancing
- Carbon-neutral infrastructure with 100% renewable energy matching
- Sovereign cloud options for specific country requirements

### Projects and organization
- Organization node is root of GCP resource hierarchy
- Folders group projects for policy application
- Projects contain resources and services with billing account
- Each project has unique ID and number for API usage
- Resource hierarchy inherits IAM policies from parent
- Labels organize resources within project for billing
- Project quotas limit resource consumption
- Multiple billing accounts can be linked to projects
- Organization policy service enforces constraints
- Project creation controlled by IAM permissions

### Billing structure
- Billing accounts linked to one or more projects
- Self-serve billing with credit card or automatic bank transfer
- Invoiced billing for enterprise customers
- Budgets and alerts notify on spending thresholds
- Billing export to BigQuery for analysis
- Committed use discounts for predictable workloads
- Sustained use discounts automatically apply
- Preemptible VMs at 60-91% discount
- Custom machine types pay only for resources needed
- Billing roles separate cost management from resource access

### IAM model
- GCP IAM manages access control across Google Cloud
- Who (member) includes Google account, service account, Google group, G Suite/Cloud Identity domain
- Role (what) is collection of permissions
- Resource (which) is GCP service or object
- Primitive roles (Owner, Editor, Viewer) are broad, legacy
- Predefined roles are service-specific with fine-grained permissions
- Custom roles define exact permission sets
- Policy binds members to roles on resources
- Resource hierarchy inherits policies
- Audit logging tracks access attempts

### Resource hierarchy
- Organization node at top for companies
- Folders organize projects by department or environment
- Projects contain resources and services
- Resources are individual service instances
- IAM policies inherited down hierarchy
- Organization policies set constraints at any level
- Labels attach metadata to resources
- Project quotas limit resource consumption
- Project numbers are immutable identifiers
- Project IDs must be globally unique

### Cloud console
- Google Cloud Console is web-based management interface
- Dashboard shows project overview and recent activity
- Navigation menu organizes services by category
- Cloud Shell provides browser-based terminal
- APIs & Services manages enabled APIs
- IAM & Admin manages permissions
- Billing displays costs and usage
- Activity shows recent changes
- Cloud Console Mobile App monitors on mobile
- Private mode for sensitive operations

### gcloud CLI
- gcloud command-line tool manages GCP resources
- Available for Linux, macOS, Windows
- Configuration sets manage multiple environments
- Properties control default behavior
- Components extend functionality (alpha, beta, kubectl)
- gcloud auth handles authentication
- gcloud config manages settings
- gcloud compute manages Compute Engine
- gcloud container manages GKE
- Output formats include JSON, YAML, text, table

## Compute

### Compute Engine
- Compute Engine provides virtual machines on Google's infrastructure
- Machine types: predefined (n1, n2, n2d, e2, c2, m1, m2)
- Custom machine types specify exact vCPU and memory
- Sole-tenant nodes for hardware isolation
- Confidential VMs for encrypted in-use data
- Shielded VMs with UEFI and secure boot
- Preemptible VMs for short-lived, fault-tolerant workloads
- Spot VMs similar to preemptible with no max runtime
- Instance templates define VM configurations
- Instance groups manage multiple VMs

### App Engine
- App Engine is fully managed PaaS for applications
- Standard environment with language-specific runtimes
- Flexible environment with container support
- Automatic scaling based on traffic
- Traffic splitting for A/B testing
- Custom domains and SSL automatically
- App Engine cron for scheduled tasks
- Task queues for background processing
- Cloud Datastore and Cloud SQL integration
- Version management with rollback

### Cloud Functions
- Cloud Functions is serverless execution environment
- Triggers from HTTP, Cloud Storage, Pub/Sub, Firebase
- Node.js, Python, Go, Java, .NET, Ruby, PHP runtimes
- Automatic scaling to zero when idle
- Execution timeout up to 60 minutes (2nd gen)
- Gen 2 functions on Cloud Run infrastructure
- Environment variables for configuration
- Secrets integration from Secret Manager
- VPC access for private resources
- Cloud Eventarc for additional event sources

### Cloud Run
- Cloud Run runs stateless containers on serverless infrastructure
- HTTP-triggered containerized applications
- Automatic scaling from zero instances
- Pay-per-use billing based on requests and resources
- Custom domains with managed SSL
- Revision tags and traffic splitting
- Cloud Run for Anthos runs on-premises
- Minimum instance setting reduces cold starts
- Concurrent request handling per container
- Cloud Run jobs for batch processing

### GKE (Kubernetes)
- Google Kubernetes Engine (GKE) is managed Kubernetes
- Control plane managed by Google with automatic upgrades
- Node pools with different machine types
- Autopilot mode for fully managed nodes
- Standard mode for cluster configuration control
- Workload Identity for GCP service accounts
- GKE Sandbox with gVisor for isolation
- Config Sync for GitOps configuration
- Network policies with Calico
- GKE Marketplace for third-party apps

## Storage

### Cloud Storage
- Cloud Storage is unified object storage
- Buckets store objects with globally unique names
- Standard storage for frequently accessed data
- Nearline for data accessed less than once per 30 days
- Coldline for data accessed less than once per 90 days
- Archive for data accessed less than once per 365 days
- Object versioning for history preservation
- Object lifecycle management automates transitions
- Object holds prevent deletion
- Signed URLs for temporary access

### Storage classes
- Standard storage: high-performance, no retrieval fees
- Nearline storage: low-cost, 30-day minimum, retrieval fee
- Coldline storage: very low-cost, 90-day minimum, higher retrieval fee
- Archive storage: lowest-cost, 365-day minimum, highest retrieval fee
- Autoclass automatically transitions objects between classes
- Regional storage replicates within single region
- Dual-region replicates between two regions
- Multi-region replicates across multiple regions
- Object holds retain data regardless of lifecycle
- Retrieval fees apply to nearline, coldline, archive

### Persistent disks
- Persistent Disks provide block storage for Compute Engine
- Zonal persistent disk in single zone
- Regional persistent disk replicated between zones
- Standard persistent disk (HDD) for sequential I/O
- Balanced persistent disk (SSD) for general purpose
- SSD persistent disk for high performance
- Extreme persistent disk for highest performance
- Snapshots for backup and migration
- Cloning creates independent copy
- Resize without downtime

### Backup strategies
- Cloud Storage for object backups
- Persistent Disk snapshots for disk backups
- Filestore backups for file shares
- Cloud SQL backups with point-in-time recovery
- BigQuery table snapshots
- Backup for GKE protects containerized apps
- Transfer Service moves data to cloud
- Google Workspace backup options
- Third-party backup solutions in Marketplace
- Cross-region replication for DR

## Networking

### VPC architecture
- Google VPC provides global software-defined networking
- Global VPC spans all regions without peering
- Subnets are regional resources with CIDR ranges
- Default VPC created automatically with project
- Auto-mode VPC creates subnets in all regions
- Custom-mode VPC gives full control
- Firewall rules control traffic with tags/service accounts
- Routes direct traffic within VPC
- VPC Network Peering connects VPCs
- Shared VPC centralizes network administration

### Subnets
- Subnets exist within single region
- Primary IP range for VM internal addresses
- Secondary ranges for alias IPs (containers, services)
- Private Google Access enables API access without external IPs
- Subnet expansion without downtime
- Auto-mode subnets have /20 primary ranges
- Custom-mode subnets use any RFC 1918 ranges
- Subnet flow logs capture traffic metadata
- Regional managed proxy only subnets
- Purpose field identifies subnet use case

### Firewall rules
- Firewall rules control ingress and egress traffic
- Direction (ingress/egress) specifies traffic flow
- Priority determines rule evaluation order
- Action (allow/deny) for matched traffic
- Targets (instances by tags or service accounts)
- Sources (IP ranges, tags, service accounts)
- Protocols and ports specified
- Implied deny at lowest priority
- Firewall insights identify unused rules
- Hierarchical firewall policies at organization/folder

### Load balancing
- Cloud Load Balancing is global, distributed service
- External HTTP(S) Load Balancer for web traffic
- External TCP/UDP Load Balancer for non-web
- Internal HTTP(S) Load Balancer for internal apps
- Internal TCP/UDP Load Balancer for private services
- SSL Proxy Load Balancer for SSL termination
- TCP Proxy Load Balancer for TCP optimization
- Backend services define instance groups
- Health checks monitor instance health
- Content-based routing directs traffic

### Cloud CDN
- Cloud CDN caches content at Google edge locations
- Integrated with HTTP(S) Load Balancing
- Cache keys customizable based on headers
- Cache modes: USE_ORIGIN_HEADERS, FORCE_CACHE_ALL, CACHE_ALL_STATIC
- TTL configuration per content type
- Signed URLs and cookies for premium content
- Cache invalidation for content updates
- Negative caching for error responses
- Cloud CDN logs for analytics
- CDN Interconnect for third-party CDNs

### Interconnect
- Cloud Interconnect connects on-premises to Google
- Dedicated Interconnect with 10 or 100 Gbps circuits
- Partner Interconnect via service provider
- VLAN attachments provision multiple connections
- Cross-Cloud Interconnect to other cloud providers
- SLA up to 99.99% availability
- Cloud Router dynamic routing with BGP
- Redundant connections for HA
- Direct Peering for Google services
- Carrier Peering via partner

### DNS
- Cloud DNS is managed authoritative DNS service
- Public zones for internet-facing domains
- Private zones for VPC internal resolution
- Forwarding zones to forward queries
- Peering zones for hybrid environments
- DNSSEC for security
- Routing policies (geo-location, weighted, failover)
- Cloud Logging for DNS queries
- Terraform support for infrastructure as code
- 100% uptime SLA

## Databases

### Cloud SQL
- Cloud SQL is fully managed relational database
- Supports MySQL, PostgreSQL, SQL Server
- Automated backups with point-in-time recovery
- Read replicas for read scaling
- High availability with regional failover
- Data encryption at rest and in transit
- Cloud SQL Auth Proxy for secure access
- Maintenance windows for updates
- Export/import to Cloud Storage
- Connection pooling with Cloud SQL Connectors

### BigQuery
- BigQuery is serverless data warehouse
- Columnar storage with automatic compression
- SQL queries across petabytes of data
- Separation of storage and compute
- BI Engine for in-memory analytics
- BigQuery ML for machine learning
- BigQuery Omni for multi-cloud analytics
- Streaming ingestion for real-time data
- Authorized views for fine-grained access
- Slot reservations for predictable performance

### Firestore
- Firestore is NoSQL document database
- Real-time listeners for live updates
- ACID transactions across documents
- Automatic multi-region replication
- Strong consistency for queries
- Collection and document hierarchy
- Firebase SDK for mobile/web apps
- Security rules for access control
- Firestore Emulator for local development
- Export/import to Cloud Storage

### Spanner
- Spanner is globally distributed relational database
- SQL queries with ACID transactions
- Automatic sharding and replication
- High availability with 99.999% SLA
- Strong consistency across regions
- Horizontal scaling without downtime
- Interleaved tables for hierarchical data
- Change streams for CDC
- Spanner Graph for graph workloads
- Multiple language clients

### Memorystore
- Memorystore provides managed Redis and Memcached
- Redis for data structures and persistence
- Memcached for simple caching
- High availability with failover
- Auto-scaling for Redis clusters
- VPC integration for private access
- Cloud Monitoring integration
- AUTH for Redis authentication
- Import/export for backup
- Redis 7+ support

## Security

### IAM roles
- Basic roles (Viewer, Editor, Owner) for simple access
- Predefined roles for specific services
- Custom roles with granular permissions
- Service accounts for application identity
- Workload Identity Federation for external identities
- IAM Conditions for context-aware access
- Deny policies (in preview) for explicit blocks
- Role recommendations from Policy Intelligence
- Role bindings at organization, folder, project, resource
- Audit logging tracks IAM changes

### Service accounts
- Service accounts are application identities
- Automatically created for Google services
- User-managed for custom applications
- Keys for authentication (JSON, P12)
- IAM roles grant permissions to service accounts
- Workload Identity for GKE to GCP
- Service account impersonation
- Service Account Key Expiry
- Service Account Token Creator role
- Disable unused service accounts

### KMS
- Cloud KMS manages encryption keys
- Symmetric and asymmetric keys
- Automatic key rotation
- Key versions for lifecycle
- HSM protection with Cloud HSM
- External Key Manager for on-prem keys
- IAM for key access control
- Audit logging for key usage
- CMEK for customer-managed keys
- CSEK for customer-supplied keys (legacy)

### Security Command Center
- Security Command Center centralizes security management
- Asset inventory and discovery
- Vulnerability scanning for Compute Engine
- Web Security Scanner for application vulnerabilities
- Threat detection with Event Threat Detection
- Container Threat Detection for GKE
- Security health analytics
- Compliance reporting (CIS, PCI, ISO)
- Security marks for asset annotation
- Integration with SIEM/SOAR

### Identity management
- Cloud Identity as identity provider
- G Suite integration
- Third-party identity federation (SAML, OIDC)
- Multi-factor authentication
- Session management and revocation
- Identity Platform for customer identity
- Google sign-in for consumer apps
- Risk assessment for suspicious logins
- Passwordless authentication options
- Device management for compliance

## Monitoring & DevOps

### Cloud Monitoring
- Cloud Monitoring collects metrics and logs
- Workspaces organize monitoring resources
- Dashboards visualize system health
- Alerting policies notify on conditions
- Uptime checks monitor external endpoints
- Metric types from Google and custom sources
- Monitoring Query Language (MQL)
- Service monitoring for SLOs
- Cloud Console integration
- API for programmatic access

### Cloud Logging
- Cloud Logging stores and analyzes logs
- Log buckets for different retention
- Log views control access
- Log-based metrics generate metrics from logs
- Log analytics with SQL queries
- Error Reporting groups similar errors
- Log exports to Cloud Storage, BigQuery, Pub/Sub
- Log routers filter and route logs
- Audit logs for compliance
- Log exclusions to reduce volume

### Deployment Manager
- Deployment Manager is infrastructure as code
- YAML and Python templates for resources
- Declarative configuration
- Preview before deployment
- Updates with modified configs
- Manifests track deployed resources
- Type providers for non-Google resources
- Composite types for reusability
- Deployment Manager integration with IAM
- Rollbacks on failure

### CI/CD pipelines
- Cloud Build for CI/CD automation
- Source repositories (Cloud Source Repositories, GitHub, Bitbucket)
- Build triggers on source changes
- Docker image builds with Kaniko
- Cloud Run and GKE deployments
- Cloud Deploy for delivery pipelines
- Deployment strategies (canary, blue/green)
- Artifact Registry for container storage
- Container Registry for legacy
- Cloud Build private pools for VPC access

## Advanced GCP

### Multi-region architecture
- Cloud Load Balancing for global traffic distribution
- Spanner for global database with strong consistency
- Cloud CDN for global content delivery
- BigQuery multi-region datasets
- Cloud Storage dual-region and multi-region
- Firestore multi-region replication
- Cloud Interconnect for hybrid connectivity
- Traffic Director for service mesh across regions
- Global VPC for unified networking
- Cloud DNS with geo-routing policies

### Hybrid cloud
- Cloud Interconnect for dedicated connectivity
- Google Distributed Cloud (Anthos) on-premises
- GKE Enterprise for consistent Kubernetes
- Config Sync for GitOps across environments
- Migrate for Compute Engine (formerly Velostrata)
- BigQuery Omni for multi-cloud analytics
- Apigee hybrid for API management
- Google Cloud Marketplace on-premises
- Cloud Run for Anthos
- Google Cloud Services Platform (legacy)

### Anthos
- Anthos is hybrid/multi-cloud platform
- GKE on-premises (bare metal and VMware)
- GKE on AWS (limited)
- GKE on Azure (limited)
- Anthos Config Management for policy
- Anthos Service Mesh (Istio-based)
- Binary Authorization for security
- Cloud Run for Anthos
- Migrate for Anthos (VM to container)
- Anthos clusters on Google Cloud

### Serverless architecture
- Cloud Run for containerized apps
- Cloud Functions for event-driven functions
- App Engine for traditional web apps
- Eventarc for event routing
- Workflows for orchestration
- Cloud Scheduler for cron jobs
- Cloud Tasks for distributed queues
- Pub/Sub for messaging
- Cloud Scheduler + Cloud Functions for scheduled tasks
- AlloyDB Omni for PostgreSQL on-premises

### Data analytics architecture
- BigQuery for data warehousing
- Dataflow for stream/batch processing
- Dataproc for managed Spark/Hadoop
- Pub/Sub for event ingestion
- Data Fusion for ETL/ELT
- Cloud Composer for workflow orchestration
- Looker for business intelligence
- Dataprep for data preparation
- Vertex AI for machine learning
- BigLake for data lakehouse

### Cost optimization
- Committed Use Discounts for predictable workloads
- Sustained Use Discounts automatically applied
- Preemptible VMs for fault-tolerant workloads
- Spot VMs for flexible, interruptible workloads
- Recommender for idle resource identification
- BigQuery slot commitments
- Cloud Storage lifecycle policies
- Network tier choices (Premium vs Standard)
- Custom machine types for right-sizing
- Budgets and alerts for cost control

### Performance tuning
- Cloud Profiler for CPU/heap profiling
- Cloud Trace for latency analysis
- Cloud Debugger for code inspection
- Cloud Monitoring for performance metrics
- Load testing with Distributed Cloud
- Compute Engine performance optimization
- BigQuery query optimization
- Cloud CDN for content caching
- Cloud Memorystore for caching
- VPC Flow Logs for network analysis

- 
